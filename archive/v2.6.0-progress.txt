=== PROJECT PROGRESS LOG ===
项目：MA150 Tracker → Workbench → Cockpit
注：详细进度以 docs/需求/features.json 各 feature 的 iteration_history + git commits 为权威，本文件仅记最近里程碑。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-06-10] project-init 迭代模式：F220 登记完成（v2.6）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 本次完成：
  - features.json 登记 F220「Fundamentals 估值指标增强 — 正常化 P/E 体系」
    · phase=design_needed / status=not_started / priority=P0
    · 预拆 5 sub-sprint：F220-a 正常化P/E核心(P0) / F220-b P/(FCF−SBC)双版本+红旗(P0)
      / F220-c 时序表预埋(P0) / F220-d EPS加速度(P1) / F220-e 预期修正方向(P1)
    · dependencies [F102, F104, F218]
    · 2 张新表 normalized_pe_history(alembic 026) + analyst_estimate_snapshots(alembic 027)
  - _iteration_log 追加 v2.6；_pipeline_status.current_iteration → v2.6
  - PRD.md 追加「v2.6 迭代」版本记录

⚠️ 登记期发现并修正：
  - 计划原 dependencies [F107,F111,F218] 有误 —— F111 不存在、F107 是纯图表 feature
    与估值无关；用户 2026-06-10 确认改为 [F102,F104,F218]

📌 关键约束（实现时守住）：
  - 正常化计算模块放 services/（workbench 侧），禁 import cockpit/pool_helpers
  - 防循环错误：平均有效税率用税率自身边界筛种子，非复用净利润异常判定
  - 覆盖仅 watchlist+pool 成员门控；降级显式不可用，绝不回退 raw

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-06-10] system-design 变更协议完成（4 文档 confirmed @ 2026-06-10）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 本次完成：
  - API-CONTRACT §GET /fundamentals 扩展正常化字段 + traceability 子对象（保留 priceToEarnings 作 raw）
  - DATA-MODEL +§NormalizedPeHistory(026) +§AnalystEstimateSnapshot(027) + DailyPayloadCache F220 复用注记
  - DECISIONS +D104(税率防循环) +D105(市值自算口径+红旗) +D106(成员门控+pool只读访问) +D107(降级不回退raw)
  - ARCHITECTURE +§Normalized Valuation 模块（禁 import cockpit）+ analyst-estimates 端点/常量 + weekly 周一07:00UTC cron
  - features.json F220 contract.decision_refs → D104-D107；4 文档 frontmatter confirmed_at=2026-06-10

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-06-10] feature-dev A-1：F220-a1 Sprint Contract confirmed
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 本次完成：
  - F220-a 全量 7 文件超 6 文件 → 拆 F220-a1 后端核心(4) + F220-a2 前端(3)，同 F219 a/b
  - F220-a1 Sprint Contract confirmed（docs/开发/sprint-contracts/F220-a1-contract.md）
    范围 = 正常化 P/E 主锚后端闭环：normalized_valuation.py 纯函数（税率防循环/异常季NOPAT/TTM/4种降级）
    + get_fundamentals 成员门控 + _is_pool_member 直读 CockpitPoolCache + _resolve_current_price 内联(禁import cockpit)
    + schema TraceabilityBlock/AbnormalQuarter + test。明确排除 P/FCF(b)/时序(c)/加速度(d)/预期(e)/前端(a2)
  - features.json：sub_sprints F220-a→a1/a2；F220-a1 contract_agreed；父 F220 in_progress；active_sprint=F220-a1
  - ⚠️ API-CONTRACT.md 未随本批提交（混入先前遗留的 Filing Events Tape 末尾改动，需用户先处理）

[下一步]（新 session，Generator）
  继续开发 F220-a1：读 SESSION-HANDOFF.md + F220-a1-contract.md，进 Generator 从开发步骤 1 开始。
  开发顺序：normalized_valuation.py 纯函数 → 单测(防循环优先) → service 编排(_is_pool_member/_resolve_current_price/门控/fail-open) → schema。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-06-10] feature-dev Generator + acceptance：F220-a1 实现完成但验收发现设计偏差 → 回退合约
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Generator + Evaluator 完成（代码保留，未提交 held）：
  - normalized_valuation.py 纯函数（normalize / 防循环税率 D104 / classify_quarters / compute_pe + 降级）零 import cockpit
  - stock_detail_service.get_fundamentals 成员门控编排 + _is_pool_member(直读 CockpitPoolCache) + _resolve_current_price 内联(禁 import cockpit)
  - schemas Fundamentals +normalizedPe/Eps/TtmEarnings + TraceabilityBlock/AbnormalQuarter
  - test_normalized_valuation.py 29 用例全过；相邻 62 过；全量 1294 passed / 11 pre-existing baseline 失败(stash 验证)
  - DECISIONS D108（实现细节）；conftest.py 加 FakeFMP.get_income_statement_quarterly 桩（合约外第 5 文件，测试必需）

❌ acceptance NOT PASS → F220-a1 needs_review 回退 contract_agreed：
  - live DUOL 实测（真实 FMP，read-only）：normalizedPe=45.3×（设计本意 ~28×、B1 要求[25,30]×），raw 13.06× ✅
  - 根因：classify_quarters 20% NOPAT 阈值把「一次性项目」和「结构性经常性非经营收入」混为一谈
    DUOL 每季 ~$8-12M 利息收入 + 多个低税季 → NI 结构性高出 NOPAT 22%+ → 8 季全判异常(唯 2025Q3=8.40 真离群)
    → 全量 NOPAT 替代，TTM 正常化盈利 $422M→$127M，P/E 虚高
  - 代码忠于合约，偏差在合约算法本身。字段名/fail-open live 验证通过(坐实假设1/待查2)
  - 验收记录：docs/验收/v2.6-F220-a1-acceptance.md；合约追加 §Contract 修订(status→revision_needed)

📌 不相关发现：consistency-check 报 F203 status drift(done 但 sub_features b1/b2 未完) → 转独立任务 task_f1a667c4

📊 5 样本跨公司探针（DUOL/NKE/NVO/NTDOY/GRAB，read-only live FMP）→ 三层系统性问题坐实：
  ① 货币/ADR：NTDOY(JPY)+NVO(DKK) 5只里2只，price 美元↔报表外币 → 静默错值(0.1×/1.5×)，fail-open 救不了
  ② classify 绝对阈值不普适：DUOL 灾难/NKE·NVO 无害/GRAB 乱跳，无共同断点 → 方向 a 证伪
  ③ 防循环税率对转盈公司(GRAB)筛非代表性低率 3.3%

✅ 用户拍板（2026-06-10）+ 已固化文档：
  1. classify 口径 → 方向 c：正常盈利=经营利润+经常性非经营收入(默认 GAAP NI)，NOPAT 仅剥真一次性项目
  2. 加货币闸：reportedCurrency!=USD → 降级 unsupported_currency(新枚举)，绝不出错值；ADR 折算留后续
  3. 发现固化进 docs/验收/v2.6-F220-a1-acceptance.md + 合约 §修订(含方向 c 待精化点)

✅ 最终裁决（2026-06-10，推翻方向 c）：放弃自算正常化，P/E 用 FMP raw
  - 关键事实：FMP raw priceToEarningsRatioTTM 对 5 只里 4 只(NKE/NVO/NTDOY/GRAB)本就准、自动处理货币，唯 DUOL 一次性税项虚低(13.5×)
  - 用户：『不要搞复杂，要个接近正确的 P/E』→ 主位直接用 FMP raw（get_fundamentals 现状 F104 已透传 priceToEarnings，零代码满足），DUOL 类虚低不修正(接受)
  - 范围『只砍正常化部分』：F220-a1/a2/c → deprecated；F220-b/d/e 保留待评估
  - held 代码全丢弃(rm 2 新文件 + git restore 3 backend + DECISIONS 移除 D108)，主分支零残留，baseline test_stock_detail 35 passed 恢复
  - 孤儿契约已清理(2026-06-10，4 文档加 deprecated 注记)：DECISIONS D104/D107、DATA-MODEL NormalizedPeHistory(026)+DailyPayloadCache注记、API-CONTRACT 正常化字段、ARCHITECTURE Normalized Valuation；保留 D105/D106 + 027表/analyst(F220-b/e)
  - 终稿：docs/验收/v2.6-F220-a1-acceptance.md；合约 frontmatter status→deprecated

[2026-06-11] feature-dev A-1：F220-b Sprint Contract confirmed（P/(FCF−SBC) 双版本）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

上游正常化 P/E（F220-a1/a2/c）已 deprecated，斩断原自洽红旗的锚 → A-1 前置 AskUserQuestion 三项重定义用户拍板：
  1. 砍掉 sbcSensitiveFlag（锚 normalizedPe 恒 null，红旗永不亮）
  2. P/FCF 市值分子改用 FMP key-metrics-ttm marketCap（推翻 D105 自算 Diluted×price：自洽对比需求随 normalizedPe 废弃消失 + FMP marketCap 极简且 ADR 货币自动正确）
  3. pFcf 成员门控 watchlist(active)∪pool（D106 落地，pool 直读 CockpitPoolCache model，不 import cockpit）

✅ 最终范围：pFcfRaw=marketCap/Σ4季(OCF+capex)、pFcfAdj=marketCap/(FCF−ΣSBC)，SBC 当真实股东成本
  - 无红旗 / 无新表 / 无新 endpoint / 不改 fmp_client（复用 F218 get_cash_flow_quarterly）
  - 6 文件：stock_detail_service(编排+_compute_p_fcf 内联纯函数+_is_pool_member) / schema(+pFcfRaw/pFcfAdj) / types / FundamentalsCard(双版本行) / conftest(FakeFMP 桩) / test_stock_detail
  - Generator step 0 doc-first：API-CONTRACT 移除 sbcSensitiveFlag+市值口径改 FMP+FCF 口径 / DECISIONS 修订 D105 / DATA-MODEL pFcf 并入 payload 注记
  - features.json：sub_sprints F220-b design_needed→contract_agreed，active_sprint=F220-b

[下一步]
  Generator 在新 session（contract_agreed→in_progress）：读 SESSION-HANDOFF.md + docs/开发/sprint-contracts/F220-b-contract.md，A-2 pre-flight 后从 step 0（doc-first 三文档）开始。
  按 skill A-1 铁律，本 session 停在 Contract confirmed，不进 Generator。

[2026-06-12] feature-dev A-2 Generator：F220-b step 0 doc-first（P/(FCF−SBC) 双版本）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

A-2 pre-flight 全过（checksum 4 项匹配 8dbff1fb / contract confirmed / phase contract_agreed）。
异常处置：API-CONTRACT.md 工作区有上游 F220 system-design v2.6 漏 commit 块 → 用户裁决「先独立补提再叠 doc-first」→ commit 84f9d02（provenance 归 system-design）。

phase：sub_sprints.F220-b contract_agreed → in_progress。

✅ step 0 doc-first 三文档修订（先于代码）：
  - API-CONTRACT §fundamentals：横幅加「F220-b 落地修订」权威子节（激活 pFcfRaw/Adj、砍 sbcSensitiveFlag、市值改 FMP marketCap、成员门控 D106、FCF=Σ4季(OCF+capex)/Adj=−ΣSBC、fail-open）；表中 pFcf 行市值口径改 FMP + sbcSensitiveFlag 行标砍；D105 自算说明行划除
  - DECISIONS §D105：加 F220-b 推翻横幅（砍红旗 + 市值改 FMP）；§D106：pFcf 成员门控落地 + 坐实待查-1（直读 CockpitPoolCache model）；frontmatter last_modified_by 更新
  - DATA-MODEL §DailyPayloadCache：补 F220-b 落地注记（仅新增 pFcfRaw/Adj、无新表、sbcSensitiveFlag 砍）

[下一步] Generator §1.1 后端：_compute_p_fcf 纯函数 + _is_pool_member + get_fundamentals 编排。

[2026-06-12] feature-dev A-3 Evaluator：F220-b → needs_review（全绿）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Evaluator 全部通过：
  - test_stock_detail.py 46 passed（35 baseline + 11 新：5 单测 + 6 集成）
  - _compute_p_fcf 纯函数：capex 符号/FCF≤0&FCF−SBC≤0 互不影响/<4季降级/OCF 回退 全守住
  - get_fundamentals：成员门控（非成员不调 get_cash_flow_quarterly）/pool-only 走编排/fail-open(HTTPError+不足4季)/marketCap None 短路/camelCase pFcfRaw·pFcfAdj
  - sbcSensitiveFlag 代码零残留（grep backend/app + frontend/src 空）
  - 严格 6 代码文件 = Contract §2

回归（标注预先存在，与 F220-b 无关）：
  - 后端全量 1277 passed / 11 failed —— baseline conftest 复现，涉 ai_schemas/earnings/fmp_client/regime/schema/alembic，早于本 session
  - 前端 vitest 331 passed / 22 failed —— 失败测试零引用本次文件，CockpitRegistry 等 baseline 复现

E2E #11：本环境无 FMP key → fundamentals 整体 502（非 F220-b 限制），改用一次性 render 验证（两行渲染 + 值格式化 + null→'—' + 无报错）通过即删；live 实测留 acceptance 期 DUOL。

consistency-check (interactive) C1/C4/C5 全清：父 F220 正确保持 in_progress（未误升 done）。
phase：F220-b testing→needs_review。

[下一步] acceptance skill：DUOL（需 ∈ watchlist 或 pool）live 实测 pFcfAdj 量级合理 → done。

[2026-06-12] acceptance：F220-b 部分验收（live 待用户部署环境）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step -1 consistency-check 入口闸 C1/C2/C5 全清（无 🔴）。
Step 0 冒烟探针无法执行：本环境无 FMP key → fundamentals 端点 502，FundamentalsCard 落 ErrorState。
F220-b 验收标准「DUOL live 实测量级」三前提（FMP key/DUOL∈watchlist∪pool/实时现金流）本环境不具备。

用户决定（2026-06-12）：保持 needs_review，由用户在带 FMP key 的部署环境 live 验收后再标 done。
已生成 docs/验收/v2.6-F220-b-acceptance.md（业务/视觉自动化证据 ✅ 全绿 + L1–L5 用户 live 清单）。

[下一步] 用户部署环境跑 L1–L5（DUOL pFcfAdj 量级合理）→ 全过通知 Claude 标 done；L2 异常 → feature-dev 类型 D。

[2026-06-12] acceptance：F220-b live 实测通过 → done ✅
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ 纠正前一条误判：FMP key 实际在【项目根 .env】（config.py 用 parent.parent.parent/.env 加载根 .env），非 backend/.env。live 验收可行且已完成。

发现 8001 跑的是 Docker 容器旧镜像（Jun 11，无 F220-b）→ 缺 pFcf 字段。从源码起临时 uvicorn(8002)，删今日 DUOL fundamentals same-day cache（旧 payload）后实测真实 FMP：
  - L1 DUOL pFcfRaw=13.87 / pFcfAdj=20.98（双行有值）✅
  - L2 pFcfAdj 20.98 ∈ 设计 [20,22]×，精准命中 ✅
  - L3 camelCase pFcfRaw/pFcfAdj 在、sbcSensitiveFlag 不在 ✅
  - L4 非成员 KO → pFcf null，P/E·marketCap 照常 ✅
  - L5 后端日志无报错 ✅
  数值自洽：mc 5.77B÷pFcfRaw≈FCF 416M（同 FMP freeCashFlow）、÷pFcfAdj≈275M→ΣSBC≈141M（DUOL 年 SBC 量级吻合）。

收尾：features.json sub_sprints.F220-b needs_review→done + iteration_history done 条目 + active_sprint→null；验收记录 docs/验收/v2.6-F220-b-acceptance.md 标 ✅。
父 F220 由 C1 决定保持 in_progress（存活子 d/e 仍 design_needed，未误升）。临时 uvicorn 已停。

[下一步] 部署需重建 8001 Docker 镜像纳入 F220-b（部署步骤）。F220-d/e 待后续 feature-dev。

[2026-06-12] 部署：docker compose 重建 8001/8080（F220-b 上线）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

用户决定直接 docker compose 部署（不打 tag，从 indicator_enhance 分支源码重建）。

⚠️ 部署前阻塞：pnpm build（tsc -b 严格项目引用）被 6 个【预存】TS 错误卡住（与 F220-b 无关，MarketRegime/PoolBuilder/SetupMonitor/AiNewsSummaryBar）→ 用户同意修复 → 独立 chore commit（删死代码/Th children 可选/删未用 import/去 JSX.Element 标注），pnpm build 恢复 exit 0。

部署：备份回滚镜像（:rollback-pre-f220b）→ docker compose build（前后端，烤入 F220-b）→ up -d 重建容器。
冒烟全过：
  - 后端 8001：AAPL（prod watchlist 成员）pFcfRaw=33.61/pFcfAdj=37.53（F220-b 在镜像里；自洽 SBC≈13.3B 合 AAPL 年量级）；sbcSensitiveFlag 不在
  - 前端 8080：HTTP 200
  - 后端日志：无 ERROR，alembic migration 正常，startup complete
注：生产 DB=backend/data/prod.db（DUOL 不在其 watchlist/pool，prod 上 DUOL 显示 '—' 属正常门控）。
回滚镜像 :rollback-pre-f220b 保留备用。

[2026-06-12] 收尾 F220：deprecate d/e → F220 done（全项目功能开发完成）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

用户裁决 deprecate F220-d/e，F220 收口为 done：
  - F220-d（EPS 加速度）地基=正常化 EPS 序列，随正常化方案废弃，复活需从头重设计（GAAP EPS），属净新增非收尾
  - F220-e（预期修正方向）独立可行但需 alembic 027 表+FMP analyst-estimates+weekly cron+repo/service/schema/前端，是完整新 feature（代码/表全未建）→ 如未来需要另起独立 feature

features.json：sub_sprints F220-d/e design_needed→deprecated；存活集仅 F220-b=done → C1 满足 → F220 status/phase→done + completed_at=2026-06-12；清理重度陈旧 acceptance_criteria（11→5，反映实际交付 F220-b）+ notes（陈旧正常化架构描述→准确收口说明，C6 已自检无状态关键词）；_pipeline_status.development→done。

下游文档同步（doc hygiene）：
  - API-CONTRACT §fundamentals：epsAcceleration(d)/estimateRevision(e) 横幅+表行标 deprecated；frontmatter 更新
  - DATA-MODEL §AnalystEstimateSnapshot(027) 加 deprecated 横幅（不建表）；DailyPayloadCache 注记澄清实际仅携 pFcfRaw/pFcfAdj；026/027 均不建，alembic head 停 025；frontmatter 更新

✅ 全项目状态：60 done + 1 deprecated（F103），无 in_progress/未完成 feature。stock_portal v2.6 功能开发收口。
F220-b 已 docker 上线（8001/8080）；回归基线干净（后端 1288 / 前端 353 全绿）。

[下一步可选] 正式发版（project-commiter 打 tag v2.6.0 + CHANGELOG，跑 strict consistency-check 闸门 + 并入 main）。
