# 验收记录：F209-a — AI 后端 schema 注册

**日期**：2026-04-25  
**验收人**：bolomeee  
**结论**：✅ 验收通过，phase → done  

---

## 验收结果

| # | 检查项 | 期望 | 实际 | 结论 |
|---|-------|------|------|------|
| B1 | POST /api/ai/market_narrator → 200 | 200 | 200 | ✅ |
| B1 | data.taskType | "market_narrator" | "market_narrator" | ✅ |
| B1 | data.schemaVersion | "v1" | "v1" | ✅ |
| B1 | meta.costUsd > 0（D072 live 验证） | > 0 | 0.00049575 | ✅ |
| B1 | output 包含 headline/summary/riskPosture/preferredSetups/avoid/warnings | ✅ | 全部字段 | ✅ |
| B2 | POST /api/ai/setup_explainer → 200 | 200 | 200 | ✅ |
| B2 | output 包含 label/quality/whyWatch/mainRisks | ✅ | 全部字段 | ✅ |
| B2 | meta.costUsd > 0 | > 0 | 0.0002423 | ✅ |
| B3 | 缺 marketScore → 422 VALIDATION_ERROR | 422 | 422 + 清晰错误 | ✅ |
| B4 | ticker 小写 → 422 VALIDATION_ERROR | 422 | 422 + pattern mismatch | ✅ |

---

## 测试门禁（Evaluator 通过记录）

| 测试组 | 通过 / 总计 |
|--------|-----------|
| §A Schema 字段约束 | 23 / 23 ✅ |
| §B Registry 注册 | 6 / 6 ✅ |
| §C Guardrail + 6 条禁词 | 12 / 12 ✅ |
| §D Endpoint 端到端（mock LiteLLM） | 6 / 6 ✅ |
| 全量回归 | 587 / 587 ✅ |

---

## 验收过程中发现的问题

### 问题 1：`ai_memos` 表不存在（500 Internal Server Error）

- **现象**：首次 POST /api/ai/market_narrator 返回 500，日志显示 `no such table: ai_memos`
- **根因**：开发数据库停在 migration 011（F203-b1），F208-a 的 migration 012（创建 ai_memos 表）未运行
- **修复**：`alembic upgrade head` — 运行 migration 012，创建 `ai_memos` 表
- **影响**：与 F209-a 代码无关，属数据库迁移疏漏
- **结论**：已修复，不影响验收结论

---

## 决策确认

| 决策 | 状态 |
|------|------|
| D074：schema 字段名 camelCase（与 API-CONTRACT 一致） | ✅ 接受 |
| 约束-2：SYSTEM_PROMPT 写入常量，暂不接入 LiteLLM | ✅ 接受 |

---

## 下一步

F209-a ✅ done → 可进入 **F209-b（Market Narrator 前端集成）**。

后端接口已就绪：
- `POST /api/ai/market_narrator`（MarketNarratorInput → envelope with MarketNarratorOutput）
- `POST /api/ai/setup_explainer`（SetupExplainerInput → envelope with SetupExplainerOutput）
