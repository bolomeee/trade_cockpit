# 验收记录 — F217-b4 Pydantic Literal 收紧（删 PULLBACK）

> 验收日期：2026-05-16 | 版本：v2.2.0 | 验收人：用户确认

## 基本信息

| 项目 | 内容 |
|------|------|
| Feature | F217-b4 — Cockpit Phase C Pydantic Literal 收紧（向后兼容窗口关闭） |
| Sprint Contract | docs/开发/sprint-contracts/F217-b4-contract.md |
| 前置 sub_sprint | F217-a done / F217-b1 done / F217-b2 done / F217-b3 done |
| 验收结论 | ✅ 通过 |

## 变更范围

6 个 Pydantic schema 文件、6 行单行删减（全为 `-"PULLBACK", `，无逻辑改动）：

| # | 文件 | 行号 | 变更内容 |
|---|------|------|---------|
| 1 | `backend/app/schemas/cockpit/position.py` | L12 | `_VALID_SETUP_TYPES` 删 `"PULLBACK"` |
| 2 | `backend/app/schemas/cockpit/pending_order.py` | L11 | `_VALID_SETUP_TYPES` 删 `"PULLBACK"` |
| 3 | `backend/app/ai/schemas/trade_plan.py` | L45 | `TradePlanInput.setupType` 内联 Literal 删 `"PULLBACK"` |
| 4 | `backend/app/ai/schemas/candidate_ranker.py` | L42 | `CandidateInput.setupType` 内联 Literal 删 `"PULLBACK"` |
| 5 | `backend/app/ai/schemas/contradiction_detector.py` | L41 | `_SETUP_TYPE` 别名删 `"PULLBACK"` |
| 6 | `backend/app/ai/schemas/journal_assistant.py` | L52 | `_SETUP_TYPE` 别名删 `"PULLBACK"` |

diff 形态：每文件 1 增 1 删（`-"BREAKOUT", "PULLBACK", "CAPITULATION", ...` → `+"BREAKOUT", "CAPITULATION", ...`），与 b2 增量形成镜像。

**backend/app/services / routers / repositories 零改动**（纯 schema 层，无 production 逻辑变动）

## 完成标准验证（T1-T7）

| # | 测试描述 | 结果 |
|---|---------|------|
| T1 | 6 文件 PULLBACK 字面量不存在（grep 输出空，exit 1） | ✅ 通过 |
| T2 | 6 文件 CAPITULATION 字面量存在（每文件 1 行） | ✅ 通过（6/6） |
| T3 | Literal get_args() 验证：6 schema `PULLBACK=NO`（向后不兼容窗口正式关闭） | ✅ 通过（6/6） |
| T4 | Literal get_args() 验证：6 schema `CAPITULATION=YES` | ✅ 通过（6/6） |
| T5 | test_setup_snapshot_purge.py（purge_legacy_pullback 不受 Literal 删减影响） | ✅ 通过（12/12） |
| T6 | test_setup_f202a.py（JSON list[str] preferred_setups 不受 Literal 影响，含 test_s15） | ✅ 通过（27/27） |
| T7 | 全量回归：1095 passed / 8 预存失败 / 0 新增失败（= b3 done 基线完全对齐） | ✅ 通过 |

## 保留项验证

| 保留项 | 原因 | 状态 |
|-------|------|------|
| `setup_snapshot_repository.py:42-45` `purge_legacy_pullback()` 中 `"PULLBACK"` 字符串 | DB 行数据查询条件，与 Pydantic Literal 完全独立 | ✅ 字节级别不动 |
| `test_ai_gateway_e2e_f208c.py:485` `"Solid pullback to support."` reason 自由文本 | 非 Literal 构造（NP-b3-3=A），留 F217-c 视语义统一性决定 | ✅ 保留不动 |
| `test_setup_f202a.py:104` `preferred_setups=json.dumps(["BREAKOUT", "PULLBACK"])` | `list[str]` 非 Pydantic Literal，无需随 Literal 收紧变动 | ✅ T6 27/27 通过 |

## Lint

ruff check 6 文件：仅 `position.py:7 F401 pydantic imported but unused`（F206-a 遗留预存，非本 sprint 引入）。

## 协商点确认

| NP | 选项 | 结论 |
|----|------|------|
| NP-b4-1 | 不新建负断言测试文件，T3 用 get_args 验证入报告 | A ✅ |
| NP-b4-2 | 单行删字面量，保 diff 对称（与 b2 镜像） | A ✅ |
| NP-b4-3 | 不追加 DECISIONS 记录（D095 已覆盖替换语义） | A ✅ |

## 下游解锁状态

F217-b4 done 后，F217 b 系列（b1/b2/b3/b4）全部完成：
- 6 个 Pydantic schema 不再接受 `"PULLBACK"` 字面量
- **F217-c 可启动**：前端 `cockpitDecisionApi.SetupType` 类型更新 / `DecisionPanelWidget` chips（Vol z-score / Drop 5d / Reversal day）/ `SetupMonitorWidget` CAPITULATION 紫色 badge
- F217-c 依赖链：a + b1 + b2 全 done → 前端类型已有上游 schema 支持

## 预存失败（非本 sprint 引入）

基线 8 条，与 b3 验收报告完全一致：
`test_ai_schemas_f209 D1 / test_ai_schemas_f211a1 R5/R6 / test_regime_f201b S4 / test_decision_f215b AlembicRoundtrip / test_fmp_client / test_regime_f201a S14 / test_schema`
