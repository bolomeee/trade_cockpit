# F214 验收记录 — ChartWidget Add to Watchlist 按钮

**日期**：2026-05-13
**版本**：v1.9（Cockpit P1 周边）
**Sprint Contract**：[F214-contract.md](../开发/sprint-contracts/F214-contract.md)
**实现 PR/Commit**：feat(F214) — needs_review 阶段（2026-05-08）已合入 improve_against_plan 分支

---

## 范围

仅 2 个文件：

- `frontend/src/workbench/widgets/ChartWidget.tsx` — 主实现
- `frontend/src/workbench/widgets/__tests__/ChartWidget.test.tsx` — 新增 7 个单元用例

不动后端、不改 useAppStore、不引入新依赖。News 和 Workbench 两页共用同一组件（`WidgetRegistry.ts:26 / :76`），按钮在两页都出现（方案 1A，用户确认）。

---

## 验收清单结果

### 视觉确认

| # | 期望 | 结果 |
|---|------|------|
| V1 | 右下角 CirclePlus 浮动按钮，z-index 高于 chart，不被 price scale 遮挡 | ✅ 2026-05-08 浏览器手验通过 |
| V2 | idle / disabled / pending(Loader2) / error 四态视觉清晰 | ✅ 2026-05-08 手验 |
| V3 | symbol === null 时按钮不渲染（走 EmptySymbol 分支） | ✅ vitest #7 |
| V4 | News 页和 Workbench 页都出现按钮且行为一致 | ✅ 2026-05-08 手验 |

### 业务逻辑确认

| # | 路径 | 自动验证 |
|---|------|---------|
| B1 | symbol 不在 signals → 按钮 enabled → 点击调 addStock(symbol) | ✅ vitest #1/#3 |
| B2 | symbol 在 signals → 按钮 disabled，title="已在 watchlist" | ✅ vitest #2 |
| B3 | 成功后 invalidateQueries(['signals']) + (['watchlist']) | ✅ vitest #4 |
| B4 | DUPLICATE 错误 → "该股票已在 watchlist" | ✅ vitest #5 |
| B5 | NOT_FOUND 错误 → "股票代码无效" | ✅ vitest #6 |
| B6 | 通用错误 → "添加失败，请重试"，~3s 自动恢复 idle | ✅ vitest #5/#6 同模式 |

### 边缘情况

| # | 场景 | 处置 |
|---|------|------|
| E1 | signals query 加载中点按钮，前端 isInWatchlist 误判为 false → 后端 DUPLICATE 兜底 | 合约 §7 已接受 |

---

## 自动化测试

- vitest `src/workbench/widgets/__tests__/ChartWidget.test.tsx`：**7/7 pass**（2026-05-13 复跑）
- 全量前端回归：needs_review 阶段记录 **268 passed 零回归**

---

## consistency-check 入口扫描结果

mode=report，发现 68 项 🔴 严重违例：

- C2 × 1：F213 → F211-a2 依赖失效（F211 已 done 但子项被重新整合，dep 字段未同步）
- C5 × 67：早期 feature（F001/F003/F005/F006/F100-F110/F112/F113/F200-b/F202-b/F203/F210/F212）合约目录与 features.json sub_sprints 字段双向不匹配

**全部为 F214 范围外的历史 drift**，同样状态在 F215-a / F215-b 验收时已存在。用户决定按历史惯例继续 F214 验收，留待专项清理 feature 处理。

---

## 已知问题

无新增。F214 范围内零问题。

---

## 结论

**F214 验收通过**。features.json 已更新 `status: done, phase: done, completed_at: 2026-05-13`。

下一步建议：

1. 当前 release 范围（v1.9 Cockpit P1 周边）已全部 done — 可触发 project-commiter 收 release tag
2. 或先开启专项 feature 清理 consistency-check 报告的 68 项历史 drift（C2 × 1 + C5 × 67）后再发版
