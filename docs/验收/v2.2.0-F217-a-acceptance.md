# F217-a 验收记录

**日期**：2026-05-15
**Feature**：F217-a — Capitulation Reversal 后端重写（Phase C 后端）
**Sprint Contract**：docs/开发/sprint-contracts/F217-a-contract.md
**验收人**：用户确认

---

## Consistency Check 入口扫描

- 🔴 严重违例：0
- 🟡 中等违例：2（C7 iteration_history 缺 in_progress/testing 条目；C8 active_sprint_phase 陈旧）
- 🟢 轻微违例：4（C4/C5 F217-b/c 设计阶段无记录，属预期状态）
- **结论**：无严重违例，准许进入验收。C7/C8 在本次收尾中修复。

---

## 静态扫描结果

| 检查 | 结果 |
|------|------|
| `PULLBACK` 引用（setup_service.py） | 0 活跃引用 ✅ |
| `PULLBACK` 引用（cockpit_params.py） | 0 活跃引用 ✅ |
| `SETUP_CAPITULATION = "CAPITULATION"` | 存在（第 24 行）✅ |
| `_indicators.py` import | 无错 ✅ |
| `_ACTIONABLE_TYPES` | `{BREAKOUT, CAPITULATION, RECLAIM, EARNINGS_DRIFT}` ✅ |
| `_classify_setup_type` 优先级 | `BROKEN > EXTENDED > EARNINGS_DRIFT > CAPITULATION > BREAKOUT > RECLAIM > NONE`（第 115 行 docstring）✅ |
| CAPITULATION_* 字段数量 | 10 个（4 个 PULLBACK_* 已全部删除）✅ |
| chart_service ATR 重构 | `_compute_atr_series` 调用 `compute_wilder_atr`（第 76 行）✅ |
| Ruff lint（F217-a 6 文件） | **All checks passed!** ✅ |

---

## 测试结果

| 测试文件 | 结果 |
|---------|------|
| `test_capitulation_reversal.py` | **34 passed**（T1-T12 含所有 sub-test）✅ |
| `test_setup_f202a.py` | **27 passed**（test_s4 BREAKOUT / test_s15 NONE）✅ |
| 全量回归 `pytest tests/` | **1084 passed，7 pre-existing failed，0 新增失败** ✅ |

### 全量回归预先存在失败说明（不阻塞）

| 测试 | 根因 | 与 F217-a 关系 |
|------|------|--------------|
| `test_regime_f201a::test_s14` | `SHARED.INDEX_ETFS` 期望 3 实际 4（VXX 在 52cdc4d 加入，早于 F217-a）| 无关 |
| `test_regime_f201b::test_s4` | 同上 | 无关 |
| `test_ai_schemas_f209` (1条) | AI tier 配置变更 | 无关 |
| `test_ai_schemas_f211a1` (2条) | resolver 配置 | 无关 |
| `test_fmp_client` | FMP screener mock | 无关 |
| `test_schema::test_all_tables_created` | DB schema 表检查（F217-a 不动 DB）| 无关 |

---

## 验收结论

**通过** — 用户 2026-05-15 确认。

所有 T1-T14 完成标准达成，Ruff 零警告，全量回归零新增失败。

F217-a phase 更新为 `done`。F217-b（alembic 021 枚举迁移）待下次 Sprint Contract 协商后继续。
