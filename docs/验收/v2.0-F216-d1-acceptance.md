# 验收记录：F216-d1 — setup_snapshots weekly_stage 列

**日期**：2026-05-14
**版本**：v2.0（F216 Cockpit Phase B — Weekly Stage Layer）
**Sprint Contract**：docs/开发/sprint-contracts/F216-d1-contract.md
**类型**：DB Schema sprint（无前端/API 变更）
**结论**：✅ 验收通过

---

## 验收清单结果

### Schema 正确性

| # | 标准 | 验证方法 | 结果 |
|---|------|---------|------|
| S1 | weekly_stage 列存在（col 21，INTEGER，notnull=0） | PRAGMA table_info(setup_snapshots) | ✅ 通过 |
| S2 | alembic upgrade → downgrade → upgrade 三段可重入 | alembic 命令 + PRAGMA 确认 | ✅ 通过 |
| S3 | ORM weekly_stage=2 与 None 均可 commit + 回读 | 临时 sqlite 内存 DB 脚本 | ✅ 通过 |
| S4 | down_revision = "019_f216b_weekly_stage_snapshots" | 文件静态确认 | ✅ 通过 |

### 文档正确性

| # | 标准 | 验证方法 | 结果 |
|---|------|---------|------|
| D1 | DATA-MODEL.md 新行位置：up_down_volume_ratio 之后、scanned_at 之前 | 人工读取 L455-L457 | ✅ 通过 |
| D2 | weekly_stage 行描述含 0=UNKNOWN/NULL 语义 + F216-d2 门禁预告 | 人工读取 | ✅ 通过 |
| D3 | 业务规则段 ready_signal 描述未提前写 weekly_stage==2 门禁 | 人工读取 L471-477 | ✅ 通过 |

### 回归不破坏

| # | 标准 | 验证方法 | 结果 |
|---|------|---------|------|
| R1 | test_setup_f202a.py 27/27 通过，API 字段集合未增加 weeklyStage | pytest 单跑 | ✅ 通过 |
| R2 | 全量 pytest 1019 passed，7 项 pre-existing failures（非本 sprint 引入） | pytest 全量 | ✅ 通过 |

---

## 本 sprint 变更文件（3 个）

| 文件 | 变更类型 |
|------|---------|
| `backend/alembic/versions/020_f216d1_setup_weekly_stage_column.py` | 新建：add_column / drop_column |
| `backend/app/models/setup_snapshot.py` | 追加 `weekly_stage: Mapped[int | None]` |
| `docs/系统设计/DATA-MODEL.md` | §SetupSnapshot 字段表追加 weekly_stage 行 |

## 顺带清理（孤立文件删除）

- `backend/alembic/versions/011_f203b_user_settings.py`（未提交，导致 alembic multiple heads）
- `backend/tests/test_decision_f203b.py`（未提交，导入不存在的 DecisionService，阻止 pytest 收集）

---

## 提交记录

| Commit | 描述 |
|--------|------|
| `37c4c20` | `wip(F216-d1): alembic 020 + setup_snapshot weekly_stage column` |
| `9fe4ce4` | `feat(F216-d1): setup_snapshots add weekly_stage column for stage gate` |

---

## 衔接说明

F216-d1 done 后直接可起 **F216-d2**：
- 范围：`setup_service._compute_ready_signal` 加 `weekly_stage==2` 强制门禁 + service join + `_row_to_dict` 输出 `weeklyStage` + schema + cockpit_params + API-CONTRACT + DECISIONS D093（~6 文件）
- 依赖：F216-d1 已提供 `setup_snapshots.weekly_stage` 存储槽位 ✅
