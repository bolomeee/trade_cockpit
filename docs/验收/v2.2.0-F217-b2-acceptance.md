# F217-b2 验收记录

> 验收日期：2026-05-16 | 版本：v2.2.0 | 验收人：用户确认
> Feature：F217 Cockpit Phase C — Capitulation Reversal 严格重写
> Sub-sprint：F217-b2 — Pydantic Literal 扩展（加 CAPITULATION，暂留 PULLBACK）

---

## 验收结论

**验收通过** ✅

---

## 范围

6 个 Pydantic schema 文件的 `setup_type` / `setupType` Literal 元组中插入 `"CAPITULATION"`，紧邻 `"PULLBACK"` 之后（NP-b2-2=A），PULLBACK 暂保留向后兼容（b4 才删）：

| # | 文件 | Literal 形式 | 改动 |
|---|------|-------------|------|
| 1 | `backend/app/schemas/cockpit/position.py` | `_VALID_SETUP_TYPES` 别名 | +CAPITULATION |
| 2 | `backend/app/schemas/cockpit/pending_order.py` | `_VALID_SETUP_TYPES` 别名 | +CAPITULATION |
| 3 | `backend/app/ai/schemas/trade_plan.py` | `TradePlanInput.setupType` 内联 | +CAPITULATION |
| 4 | `backend/app/ai/schemas/candidate_ranker.py` | `CandidateInput.setupType` 内联 | +CAPITULATION |
| 5 | `backend/app/ai/schemas/contradiction_detector.py` | `_SETUP_TYPE` 别名 | +CAPITULATION |
| 6 | `backend/app/ai/schemas/journal_assistant.py` | `_SETUP_TYPE` 别名 | +CAPITULATION |

---

## 测试结果

| # | 测试 | 结果 | 备注 |
|---|------|------|------|
| T1 | 6 schema imports 无报错 | ✅ 通过 | `uv run python -c "from app.schemas..."` |
| T2 | PULLBACK 向后兼容（18 条测试） | ✅ 18/18 passed | `pytest -k "PULLBACK or pullback"` |
| T3 | cockpit schemas 接受 CAPITULATION | ✅ 通过 | PositionCreate + PendingOrderCreate REPL 验证 |
| T4 | AI schemas 接受 CAPITULATION | ✅ 通过 | TradePlanInput / CandidateInput / ContradictionDetectorInput / ClosedTradeBrief / TradeReviewPayload |
| T5 | 6 文件均含 CAPITULATION | ✅ 通过 | 4 同行 + 2 多行 Literal，grep 全部确认 |
| T6 | 6 文件仍含 PULLBACK | ✅ 通过 | 每文件 ≥1 处（`grep -c` 确认） |
| T7 | 全量回归 | ✅ 通过 | 1095 passed，8 预存失败，**0 新增失败** |

---

## 静态检查

| 检查 | 结果 |
|------|------|
| ruff lint | ⚠️ position.py F401 `import pydantic`（预存 F206-a，非本 sprint 引入） |
| SYSTEM_PROMPT 未变更（NP-b2-4=A） | ✅ 4 个 AI schema SYSTEM_PROMPT 均无 CAPITULATION |
| 无新建测试文件（NP-b2-1=A） | ✅ 6 文件命中上限，全部为修改 |
| CAPITULATION 位置一致（NP-b2-2=A） | ✅ 所有 6 处均为 `"PULLBACK", "CAPITULATION"` 顺序 |
| 无共享 Literal 别名模块（NP-b2-3=A） | ✅ YAGNI，未引入跨包依赖 |

---

## Consistency Check

入口扫描：severe=0 medium=0 minor=4（均为预期：F217-b3/b4/c design_needed 无合约/history）

---

## NP 确认

| # | 议题 | 选项 | 结论 |
|---|------|------|------|
| NP-b2-1 | 是否新建测试文件 | A. 不新建 | ✅ A（依赖 b3 fixture 迁移覆盖） |
| NP-b2-2 | CAPITULATION 在 Literal 中的位置 | A. 紧邻 PULLBACK 之后 | ✅ A（最小 diff，b4 删 PULLBACK 时对齐） |
| NP-b2-3 | 是否抽共享别名模块 | A. 保留现状 | ✅ A（YAGNI） |
| NP-b2-4 | 是否改 SYSTEM_PROMPT | A. 不改 | ✅ A（prompt 行为合约属 F217-c） |

---

## 已知问题

- ruff F401（position.py `import pydantic`）：预存问题，F206-a 引入，不影响运行时。

---

## Git 记录

```
wip(F217-b2): cockpit schemas Literal +CAPITULATION (keep PULLBACK)
wip(F217-b2): trade_plan Literal +CAPITULATION
wip(F217-b2): candidate_ranker Literal +CAPITULATION
wip(F217-b2): contradiction_detector Literal +CAPITULATION
wip(F217-b2): journal_assistant Literal +CAPITULATION
feat(F217-b2): Pydantic schemas Literal +CAPITULATION (PULLBACK kept for b3 window)
```

---

## 后续

- **F217-b3**（next）：测试 fixture 批量去 PULLBACK（6 文件：test_setup_f216d2 / test_pool_service / test_ai_schemas_f211a1 / test_f215a / test_regime_f201b / test_decision_f203b2）
- **F217-b4**：Pydantic Literal 删 PULLBACK 收紧（b3 完成后才能跑）
- **F217-c**：前端 cockpitDecisionApi 类型 / DecisionPanelWidget chips / SetupMonitorWidget CAPITULATION 紫色 badge
