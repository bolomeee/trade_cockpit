# 验收记录：F205-d PoolBuilderWidget 前端

**日期**：2026-04-28
**版本**：v1.9.0
**Feature**：F205-d（父：F205 Pool Builder Widget）
**验收人**：用户确认
**结论**：✅ 通过

---

## 实现范围

| 文件 | 状态 |
|------|------|
| `frontend/src/cockpit/lib/api/cockpitPoolApi.ts` | 新建 |
| `frontend/src/cockpit/widgets/PoolBuilderWidget.tsx` | 新建 |
| `frontend/src/cockpit/widgets/_poolFilterBar.tsx` | 新建 |
| `frontend/src/cockpit/CockpitRegistry.ts` | 修改（新增 `cockpit.pool-builder`） |
| `frontend/src/cockpit/widgets/__tests__/PoolBuilderWidget.test.tsx` | 新建 |
| `frontend/src/cockpit/lib/api/__tests__/cockpitPoolApi.test.ts` | 新建 |

---

## 测试结果

| 测试类型 | 通过 | 总数 |
|---------|------|------|
| API client 单元测试 | 5 | 5 |
| Widget 组件测试 | 11 | 11 |
| TypeScript 编译 | ✅ 0 错误 | — |

**测试 bug 修复（验收期间）**：T6 测试使用 `getByText('XLK')` 因 sector 同时出现在表格行和 Filter Bar 下拉选项中而报 "multiple elements"，改为 `getAllByText` 规避（行为正确，测试查询需要更宽松）。

---

## 验收确认项

### 视觉
- ✅ Widget 出现在 Cockpit，位于 ActionList 下方
- ✅ Funnel 横向 5 段（Tradable/Trend/RS/Fund./Action），千分位格式
- ✅ Funnel 段点击高亮（aria-pressed 切换）
- ✅ Filter Bar 9 个字段（MktCap/Price/ADV/Trend/RS/RevGrow/Sector/Setup/Limit）
- ✅ Sector 下拉自动填充数据中的 sector
- ✅ 候选表 13 列齐全
- ✅ null 字段显示 `—`（D080 规则）
- ✅ Action 颜色 token 正确
- ✅ 无硬编码颜色，数字列等宽字体

### 业务逻辑
- ✅ Pool 数据加载正常
- ✅ Filter 改变 → 300ms debounce 后重新请求，不刷整页
- ✅ `[+]` 按钮 → 调 addStock + invalidate cockpit-pool + watchlist
- ✅ inWatchlist=true 初始灰态
- ✅ 错误状态显示 "Failed to load pool data"

### 边缘情况
- ✅ 空态显示 "No candidates"
- ✅ Sector 下拉过滤有效
- ✅ Limit 参数有效

---

## 已知限制（不影响验收）

1. **Funnel 段只高亮，不切换内容**：后端目前只返回 action 层 items。各层明细切换延至后续 sprint（Sprint Contract §排除 明确说明）。
2. **Ignore / Remove 按钮未实现**：父 feature acceptance_criteria 提及，但 F205-d Sprint Contract 未包含。确认接受，后续版本补充。

---

## F205 整体状态

所有子 sprint 已完成：F205-a ✅ → F205-b ✅ → F205-c ✅ → F205-d ✅

**F205 Pool Builder Widget：phase = done**
