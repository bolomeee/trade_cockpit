# 验收记录 — F218-d6a T5 数据层

> **版本**：v2.4-dev | **Feature**：F218-d6a | **日期**：2026-05-20
> **Sprint Contract**：[F218-d6a-contract.md](../开发/sprint-contracts/F218-d6a-contract.md)
> **验收类型**：纯后端 sprint（无视觉验收项）

---

## Consistency-Check 入口扫描

| 严重度 | 违例数 | 结论 |
|--------|--------|------|
| 🔴 严重 | 0 | 全清 |
| 🟡 中等 | 0 | 全清（C7 跳步均由项目 `_phase_transition_convention` 覆盖） |
| 🟢 轻微 | 15 | 历史遗留 / 预期（design_needed sprints 无合约属正常） |

**结论**：入口闸通过。

---

## Contract 完成标准验收（AC1–AC11）

| # | 标准 | 验证方式 | 结果 |
|---|------|---------|------|
| AC1 | `get_balance_sheet_quarterly` 调用 `/balance-sheet-statement?period=quarter&symbol=AAPL&limit=8`；成功返 list；fail-open 返 `[]` | pytest `TestFmpClientBalanceSheetQuarterly` ×2 | ✅ |
| AC2 | `get_cash_flow_quarterly` 调用 `/cash-flow-statement?period=quarter`；happy path 返非空 list | pytest `TestFmpClientCashFlowQuarterly` ×1 | ✅ |
| AC3 | 2 新方法复用既有 `_FmpRateLimiter`（不引入独立 limiter；pool_cache_service.py 无 `_FmpRateLimiter()` 实例化） | 代码检查 `grep` | ✅ |
| AC4 | `compute_fundamentals_row_from_balance_cash` happy path：NVDA Q2 2026 → `net_debt=7B, fcf=8.5B`，8-key 输出 | pytest `TestComputeFundamentalsRow::test_happy_path` | ✅ |
| AC5 | 空值安全：totalDebt/cash/freeCashFlow null → 对应字段 null；BS/CF id 不匹配/缺失 → None | pytest `TestComputeFundamentalsRow::test_null_fields_and_mismatch` ×5（参数化）+ `test_missing_id_fields_returns_none` | ✅ |
| AC6 | `compute_supplemental_key_metrics_from_is_bs_cf` happy path：fcf_margin=8.5/30≈0.2833，roic=16/57≈0.2807；输出仅 5 keys（不含 period_end_date/gross_margin 等） | pytest `TestComputeSupplementalKeyMetrics::test_happy_path` + 直接验证 key 集合 | ✅ |
| AC7 | revenue=0 → fcf_margin=None；roic 分母≤0 → None；freeCashFlow=None → fcf_margin=None；3 payload id 不匹配 → None | pytest `TestComputeSupplementalKeyMetrics` ×2 + 代码执行验证 | ✅ |
| AC8 | `FundamentalsRepository.upsert` null-not-erase：首次 `total_debt=10B, fcf=None` → 第二次 `total_debt=None, fcf=8.5B` → 最终 `total_debt=10B + fcf=8.5B`；`get_recent_for_ticker` DESC limit | pytest `TestFundamentalsRepository::test_upsert_null_not_erase_and_get_recent` | ✅ |
| AC9 | `_rebuild_fundamentals` 集成：2 ticker × 2 季 → 4 fundamentals rows + 4 supplemental km rows；BS 返 [] 该 ticker 跳过不阻断 | pytest `TestPoolCacheFundamentalsIntegration::test_rebuild_writes_both_tables_and_logs` | ✅ |
| AC10 | `rebuild()` 端到端：cockpit + key_metrics + fundamentals 三类数据正确写入 + 3 条 OK system_log；既有 cockpit 行 rs_percentile/ma50/last_close 无影响 | pytest `TestPoolCacheFundamentalsIntegration` + 全量回归 1204 passed | ✅ |
| AC11 | Alembic 024 双向：`upgrade head` 建表 + 字段类型正确；`downgrade -1` 删表；再 `upgrade head` 幂等 | `alembic upgrade head / downgrade -1 / upgrade head` CLI 执行 | ✅ |

**所有 11 项 AC 全部通过。**

---

## Evaluator 自检清单

| 类别 | 检查项 | 结果 |
|------|--------|------|
| 测试 | 15 个新测试全部通过（参数化展开 contract 预期 11） | ✅ |
| 回归 | d1/d2/d3a/d3b/d4/d5 既有 61 tests 无回归 | ✅ |
| 全量回归 | `uv run pytest` 9 failed = pre-existing baseline，无新增 | ✅ |
| Alembic | 024 双向跑通 | ✅ |
| 模型字段 | `StockFundamentalsQuarterly` 9 字段（BigInteger nullable / Date NOT NULL / DateTime NOT NULL）与 DATA-MODEL.md §1216-1235 1:1 | ✅ |
| 签名 | `get_balance_sheet_quarterly` / `get_cash_flow_quarterly` 与 `get_income_statement_quarterly` 风格 1:1 | ✅ |
| 常量位置 | `FMP_EP_BALANCE_SHEET` / `FMP_EP_CASH_FLOW` 在顶部声明区 | ✅ |
| 纯函数 | 2 helper 是模块级纯函数（无 logger / 无 DB 调用） | ✅ |
| 重构 | `_rebuild_key_metrics` 签名 → `tuple[int, dict]`；调用方仅 `rebuild()` 内部 | ✅ |
| fail-open | `_rebuild_fundamentals` BS/CF/IS 任一返 `[]` 该 ticker 跳过，整体不阻断 | ✅ |
| rate limiter | 复用既有 token bucket（pool_cache_service.py 无第二个 `_FmpRateLimiter()` 实例化） | ✅ |
| models/__init__ | `StockFundamentalsQuarterly` 注册条目存在 | ✅ |
| **null-not-erase 关键** | d3a gross/op/net_margin 不被 d6a supplemental upsert 擦除（直接 SQL 验证） | ✅ |
| **代码质量** | `_rebuild_fundamentals` 43 行 ≤ 50 行上限 | ✅ |
| 魔法值 | `limit=8` / `_FMP_MAX_WORKERS` 均为常量或入参 | ✅ |
| 副作用 | 2 helper 函数内无 logger / DB 调用 | ✅ |
| 死代码 | 无注释掉的代码块 / 死 import / 未使用变量 | ✅ |

---

## 发现的问题及处置

| 问题 | 发现时机 | 处置 |
|------|---------|------|
| `_rebuild_fundamentals` 58 行超 50 行上限 | Evaluator 代码质量检查 | 精简 docstring + lambda 替代内嵌 def → **43 行**，commit `6902942` 修复 |

---

## 结论

**✅ F218-d6a 验收通过。**

- 11 项 Contract AC 全部通过
- Evaluator 自检清单全部绿灯（包含验收期间发现并修复的 1 项代码质量问题）
- null-not-erase 关键不变量经 SQL 直接验证
- 全量回归无新增 failures

---

*验收者：用户 + Claude Sonnet 4.6 | 2026-05-20*
