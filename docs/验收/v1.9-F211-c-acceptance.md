# 验收记录：F211-c — News 页 AI 摘要 bar 前端

**日期**：2026-04-29
**Feature**：F211-c（F211 AI 层 第三段）
**Sprint Contract**：docs/开发/sprint-contracts/F211-c-contract.md
**验收结论**：✅ 通过
**验收人**：用户确认

---

## 技术门禁（Generator Evaluator 完成）

| 项 | 结果 |
|---|------|
| vitest 全量 | 273/276 pass（3 个预存 TopNav 失败不变） |
| 新增测试 | 14/14 pass（C1-C8 component + 6 helper unit tests） |
| tsc --noEmit | 0 error |
| ESLint 变更文件 | 0 新增 warning |
| consistency-check（入口） | 通过（无 F211-c 相关违例） |

---

## 视觉确认

| # | 检查项 | 状态 |
|---|--------|------|
| V1 | trigger 按钮样式（灰底/细边框/32px） | ✅ |
| V2 | trigger 位置（grid 上方，mb-3 间距） | ✅ |
| V3 | loading 态（header + 64px + 40px skeleton） | ✅ |
| V4 | success 态排列（摘要→sentiment→tickers→risks） | ✅ |
| V5 | sentiment badge 颜色（Positive绿/Neutral灰/Negative红） | ✅ |
| V6 | risks bullet list（`·` 前缀，gray caption） | ✅ |
| V7 | tickers chip（灰底/细边框/可点击） | ✅ |
| V8 | cache badge（Cached / Generated · model） | ✅ |
| V9 | 关闭按钮（✕ → 回 trigger） | ✅ |

---

## 业务逻辑确认

| # | 场景 | 状态 |
|---|------|------|
| B1 | 无 articles → trigger disabled + tooltip="暂无 news" | ✅ |
| B2 | 有 articles，点 trigger → loading skeleton | ✅ |
| B3 | AI 响应后 → 完整 success 态 | ✅ |
| B4 | 点 ticker chip → selectedSymbol 切换 | ✅ |
| B5 | 点 ✕ → 回 trigger | ✅ |
| B6 | 再次点 trigger → 命中 cache（"Cached" badge） | ✅ |
| B7 | 刷新页面 → closed 状态（不持久化） | ✅ |

---

## 边缘情况

| # | 场景 | 状态 |
|---|------|------|
| E1 | 后端 500 → "AI 暂不可用" + ✕ 回 trigger | ✅ |
| E2 | risks=[] → risks 区隐藏，其余正常 | ✅ |
| E3 | relevantTickers=[] → Tickers 行隐藏 | ✅ |

---

## 实现摘要

**新建文件（5 个）**：
- `frontend/src/components/news/newsSummaryUtils.ts` — 4 pure helpers
- `frontend/src/components/news/AiNewsSummaryBar.tsx` — 6 态状态机组件
- `frontend/src/components/news/__tests__/AiNewsSummaryBar.test.tsx` — 14 tests
- `frontend/src/pages/News.tsx` — +4 行集成
- `docs/需求/features.json` — 状态更新

**关键设计调整**（Generator 阶段）：
- `--color-surface-muted` token 不存在 → chip 背景改用 `--color-input-background`
- helpers 拆到 `newsSummaryUtils.ts`（sibling file）以解决 `react-refresh/only-export-components` ESLint error

---

## F211 整体状态

| Sub-sprint | 状态 |
|---|---|
| F211-a1 | ✅ done |
| F211-a2 | ✅ done |
| F211-b | ✅ done |
| F211-c | ✅ done（本次） |
| F211-d | ⬜ design_needed（平仓 hook + ai_review + 月度 cron） |

F211 父级 status：`in_progress`（F211-d 未完成）
