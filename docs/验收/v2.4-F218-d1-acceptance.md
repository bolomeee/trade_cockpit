# 验收记录 — F218-d1 Repricing Trigger Framework (Skeleton)

**版本**：v2.4 (in progress)
**Feature**：F218-d1 — Framework: model + alembic 022 + repository + service skeleton
**验收日期**：2026-05-18
**验收结论**：✅ 通过
**Sprint Contract**：[docs/开发/sprint-contracts/F218-d1-contract.md](../开发/sprint-contracts/F218-d1-contract.md)

---

## 验收范围说明

F218-d1 是纯后端 sprint，无 UI/API endpoint/前端改动。验收聚焦于代码正确性和测试覆盖，无视觉验收项。

---

## 技术验收结果

### 测试覆盖（14 测试 4 class，全绿）

| Class | 范围 | 结果 |
|-------|------|------|
| TestRepricingTriggerRepository (R1–R8) | upsert / soft_expire / get_active_for_ticker / get_all_active / delete_expired_inactive + ON CONFLICT UQ 三次稳定 | ✅ 8 green |
| TestRepricingTriggerServiceSkeleton (S9–S12) | 5 detector None 端到端 / soft expire 自动触发 / 错误隔离 / DetectorResult 序列化 | ✅ 4 green |
| TestRepricingTriggerMigration (M13) | alembic 022 upgrade → downgrade → re-upgrade 三向 | ✅ 1 green |
| TestRepricingTriggerConstants (C14) | TRIGGER_TYPES 5 元组字面一致 | ✅ 1 green |

全量回归：1128 passed，9 failed（全部 pre-existing，无新增失败）

### 关键设计决策确认

| 决策 | 验证方式 | 结论 |
|------|---------|------|
| NP-d1-1：soft_expire 在 detector 内同循环触发 | test_s10 通过 | ✅ |
| NP-d1-2：DetectorResult dataclass 在 service 顶部 | 代码审查 | ✅ |
| NP-d1-3：5 占位 `return None` 端到端跑通 | test_s9 通过 | ✅ |
| NP-d1-4：14 测试合并 1 文件 4 class | 文件结构确认 | ✅ |
| NP-d1-5：active tickers 复用 StockRepository（改用 list_active()） | 代码审查 | ✅ |

### 交付文件清单（6/6）

| 文件 | 类型 | 状态 |
|------|------|------|
| `backend/app/models/repricing_trigger.py` | 新增 | ✅ |
| `backend/app/models/__init__.py` | 修改 (+1 import) | ✅ |
| `backend/alembic/versions/022_f218_repricing_triggers.py` | 新增 | ✅ |
| `backend/app/repositories/repricing_trigger_repository.py` | 新增 | ✅ |
| `backend/app/services/cockpit/repricing_trigger_service.py` | 新增 | ✅ |
| `backend/tests/test_repricing_trigger_skeleton.py` | 新增 | ✅ |

---

## 偏差记录

| 偏差 | 影响 | 处理 |
|------|------|------|
| `StockRepository.get_active_tickers()` 不存在，改用 `[s.ticker for s in list_active()]` | 无功能影响，意图一致 | 验收接受，6 文件上限不变 |
| `upsert()` 需 `expire_all()` 防 identity map 缓存 | 否则 ON CONFLICT UPDATE 后返回旧值 | 已修复并测试覆盖（R2/R8） |

---

## 已知问题（不阻塞）

| 问题 | 计划修复 |
|------|---------|
| `test_schema.py::test_all_tables_created` EXPECTED_TABLES 未含 `repricing_triggers`（pre-existing 同类问题，`weekly_stage_snapshots` 早存在） | F218-d7a 一并修 |

---

## 后续 sub-sprint

下一步：**F218-d2** — T1 Earnings Acceleration detector 实装
依赖本 sprint：`RepricingTriggerService._detect_earnings_acceleration()` 占位槽 + `RepricingTriggerRepository` 持久化层。
