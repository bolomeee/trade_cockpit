# v2.6 F220-b 验收记录 — P/(FCF−SBC) 双版本（现金流交叉视角）

> 生成：2026-06-12 | 状态：**✅ 验收通过 → done**
> Feature：F220-b　phase=`done`（completed 2026-06-12）
> Sprint Contract：[F220-b-contract.md](../开发/sprint-contracts/F220-b-contract.md)
> feat commit：`dee39aa`

---

## 验收结论摘要

| 维度 | 状态 |
|------|------|
| consistency-check 入口（C1/C2/C5） | ✅ 全清，无 🔴 违例 |
| 业务逻辑 — 自动化证据 | ✅ 11 用例全覆盖（单测 + 集成） |
| 视觉渲染 — 自动化证据 | ✅ render 验证（两行 + null→'—'） |
| **live 数据实测（DUOL 量级）** | ✅ **DUOL pFcfAdj=20.98 ∈ 设计 [20,22]×，精准命中** |

> ✅ live 实测完成（2026-06-12）：FMP key 在**项目根 `.env`**（config.py 加载根 .env，早先误判「无 key」已纠正）；dev.db 中 DUOL ∈ watchlist active → 成员门控满足。从源码起临时 uvicorn（8002）实测真实 FMP 数据，L1–L5 全过。
> ⚠️ 部署提醒：8001 上跑的是 Docker 容器旧镜像（Jun 11，不含 F220-b），部署环境需**重建镜像**才能让 8001 提供 pFcf 字段（属部署步骤，非本 sprint）。

---

## 一、业务逻辑确认（已有自动化证据）

| # | acceptance_criteria | 自动化证据（test_stock_detail.py） | 结果 |
|---|---|---|------|
| B1 | 成员股票显示双版本 P/FCF | `test_fundamentals_pool_member_computes_p_fcf` | ✅ |
| B2 | pFcfRaw=marketCap/Σ4季(OCF+capex)、pFcfAdj=marketCap/(FCF−ΣSBC) 数值正确 | `test_compute_p_fcf_normal_four_quarters` | ✅ |
| B3 | capex 按符号直接加（不取绝对值/不二次取反） | `test_compute_p_fcf_capex_added_by_sign` | ✅ |
| B4 | FCF≤0→pFcfRaw null；FCF−SBC≤0→pFcfAdj null（互不影响） | `test_compute_p_fcf_nonpositive_fcf_and_adj_independent` | ✅ |
| B5 | <4 季 / 某季 OCF·capex 缺 →(None,None)；SBC 缺按 0 | `test_compute_p_fcf_degrades_on_insufficient_or_missing` | ✅ |
| B6 | OCF 用 operatingCashFlow，缺回退 netCashProvidedByOperatingActivities | `test_compute_p_fcf_ocf_fallback_field` | ✅ |
| B7 | 非成员 → pFcf null 且不调 get_cash_flow_quarterly（省配额） | `test_fundamentals_non_member_skips_cash_flow`（断言 cash_flow_calls==[]） | ✅ |
| B8 | fail-open：现金流 HTTPError / 不足 4 季 → null，endpoint 200，原始指标照常 | `test_fundamentals_fail_open_on_cash_flow_error` + `_on_insufficient_quarters` | ✅ |
| B9 | 成员但 marketCap None → pFcf null（不除零），不拉现金流 | `test_fundamentals_member_without_market_cap` | ✅ |
| B10 | 响应 camelCase pFcfRaw/pFcfAdj；sbcSensitiveFlag 不出现 | `test_fundamentals_response_has_camelcase_p_fcf_keys` | ✅ |

## 二、视觉确认（已有 render 证据）

| # | 检查项 | 证据 | 结果 |
|---|---|---|------|
| V1 | FundamentalsCard 渲染 8 指标（左 P/E·P/S·PEG·ROCE / 右 FCF·P/FCF·P/FCF(−SBC)·Float） | 一次性 render 测试（跑完即删，未入库） | ✅ |
| V2 | null pFcf → '—'（复用既有兜底，无新状态分支） | render 断言 ≥2 个 '—' | ✅ |

## 三、回归（标注预先存在）

- 后端全量 1277 passed / 11 failed —— **均预先存在**（baseline conftest 复现，涉 ai_schemas/earnings/fmp_client/regime/schema/alembic，与 F220-b 无关）
- 前端 vitest 331 passed / 22 failed —— **均预先存在**（失败测试零引用本次文件）

---

## 四、live 实测结果（2026-06-12，临时 uvicorn 8002 + 真实 FMP）

| # | 操作 | 期望 | 实测结果 | 状态 |
|---|------|------|---------|------|
| L1 | DUOL fundamentals 端点 | 出现 pFcfRaw/pFcfAdj 有值 | `pFcfRaw=13.87`、`pFcfAdj=20.98` | ✅ |
| L2 | pFcfAdj 量级 | 合理（设计标 [20,22]×） | **20.98 ∈ [20,22]，精准命中** | ✅ |
| L3 | 响应字段 | 含 pFcfRaw/pFcfAdj，无 sbcSensitiveFlag | 两字段在、sbcSensitiveFlag 不在 | ✅ |
| L4 | 非成员 KO | pFcf null，原始指标照常 | pFcf 均 null，P/E·marketCap 正常 | ✅ |
| L5 | 后端日志 | 无报错 | uvicorn 日志干净 | ✅ |

**数值自洽**：marketCap 5.77B ÷ pFcfRaw 13.87 ≈ FCF 416M（与 FMP freeCashFlow 一致）；÷ pFcfAdj 20.98 ≈ 275M → ΣSBC ≈ 141M（DUOL 年 SBC 量级吻合）。前端渲染由 Generator 阶段 render 测试证明（两行 + null→'—'）。

---

## 五、收尾

- ✅ L1–L5 全过 → F220-b phase=done（completed_at 2026-06-12）。
- 父 F220 由 consistency-check C1 决定：存活子 d/e 仍 design_needed → 父保持 `in_progress`（未误升 done）。
- 部署：8001 Docker 镜像需重建以纳入 F220-b（部署步骤，由 project-commiter / 部署流程处理）。
