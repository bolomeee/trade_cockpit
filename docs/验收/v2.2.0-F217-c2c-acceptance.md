# 验收记录 — F217-c2c

**Feature**：F217 Cockpit Phase C — Capitulation Reversal 严格重写  
**Sub-sprint**：F217-c2c（SetupType union 收紧 -PULLBACK + 清理 + design-spec）  
**版本**：v2.2.0  
**验收日期**：2026-05-18  
**验收结论**：✅ 通过

---

## 实现范围（8 文件）

- `setupMonitorApi.ts`：SetupType union 删 `| 'PULLBACK'`
- `SetupTypeBadge.tsx`：union + TYPE_COLORS + TYPE_LABELS 删 PULLBACK 三处
- `cockpitPoolApi.ts`：inline union 删 `'PULLBACK'`
- `_pendingOrderRow.tsx`：inline union 删 `'PULLBACK'`
- `_pendingOrderFormSchemas.ts`：删 PULLBACK 选项，加 `{ value: 'CAPITULATION', label: 'CAP_REV' }`
- `AiSetupExplainerPopover.tsx`：Props.setupType 删 PULLBACK 加 CAPITULATION，mapping 更新
- `tokens.css`：删 `--color-setup-pullback`
- `docs/设计/design-spec.md`：color 表 / ASCII mockup / popover 偏离 / v2.2.0 偏离说明全更新

## 验收检查结果

| 检查 | 结果 |
|------|------|
| TSC 0 错误（TS 编译期禁止 PULLBACK 字面量）| ✅ |
| cockpit src PULLBACK grep 0 命中 | ✅（AiSetupExplainerPopover L17 backend schema 例外，合同明确）|
| `--color-setup-pullback` token 0 命中 | ✅ |
| Pending Order 下拉：CAP_REV 存在 / 无 PULLBACK | ✅ 截图确认 |
| SetupMonitor badge 渲染正常（EARN_DRFT/RECLAIM 颜色正确）| ✅ 截图确认 |
| 全页面无 PULLBACK 字样 | ✅ |
| Console 无 error | ✅ |
| 全量测试套件 | ✅ 22/28 suites（= c2b 基线，0 新增失败）|

## 遗留注意事项（来自 SESSION-HANDOFF §5）

- **SetupMonitorWidget §S pre-existing 11 个失败**：根因是 widget 未渲染 AiSetupExplainerPopover，独立 sprint 处理（候选 F217-c3 / F218）
- **AiSetupExplainerPopover L17 `setup: 'pullback' | ...`** 保留：backend setup_explainer AI task input schema（F211 归口），前端无法传入 PULLBACK，安全过渡态
- **3 处 inline SetupType union 复本**（cockpitPoolApi / _pendingOrderRow / SetupTypeBadge）内容已同步，下次 setup_type 变更需手工同步

## F217 父 feature 状态

所有 sub_sprints（a / b1-b4 / c1 / c2a / c2b / c2c）全部 done → F217 升 `done`，completed_at: 2026-05-18。
