# 验收记录：F113-b — News 前端持久化 + 增量刷新

**日期**：2026-04-23  
**版本目标**：v1.7.0（F113-a + F113-b + F113-c 合并发版）  
**验收人**：用户确认  
**结论**：✅ 通过

---

## 验收项目

| # | 检查项 | 结果 |
|---|-------|------|
| L1 | 清空 localStorage 后刷新 → 发 `?window=calendar-1d` 请求，文章显示 | ✅ |
| L2 | 二次刷新 → 文章立即显示，无新 `/articles` 请求（localStorage 命中） | ✅ |
| L3 | 点击 Refresh 图标 → 发 `?since=<iso>` 请求，图标旋转 + disabled | ✅ |
| L4 | LocalStorage 存在 `ma150.news.v1.<today>` key，value 为 JSON 数组 | ✅ |
| E1 | 手动改 localStorage key 日期为昨天 → 重新 fetch | ✅ |
| E2 | Refresh 进行中不可重复点击 | ✅ |

---

## 附带改动（同 session）

- **News 表格行距/字号对齐 Watchlist**：`text-[11px] [&_td]:py-[3px]`，视觉已确认。
- **useNewsArticles hooks 修复**：`loadTodayArticles()` 改为 `useState` lazy initializer，消除 React hooks-order 警告。

---

## 下一步

F113-c（已读状态）contract 待起草，用户确认后开始实现。
