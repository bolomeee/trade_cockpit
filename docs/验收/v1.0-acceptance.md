# v1.0 验收记录

> 项目：MA150 Tracker | 起始日期：2026-04-17

---

## F000-a 后端脚手架 + 数据库基座

**验收日期**：2026-04-17
**Sprint Contract**：[docs/开发/sprint-contracts/F000-a-contract.md](../开发/sprint-contracts/F000-a-contract.md)
**commit**：`10a1e95`
**结论**：✅ 通过

### 验收执行记录

| # | 操作 | 期望 | 实际 | 状态 |
|---|------|------|------|------|
| A1 | `uv sync` 安装依赖 | 无错误 | 通过 | ✅ |
| A2 | `uv run uvicorn app.main:app --reload` 启动 | 控制台显示 running、无 ERROR | 通过 | ✅ |
| A3 | `curl /health` | `{"status":"ok"}` | 通过 | ✅ |
| A4 | 浏览器 `/docs` | Swagger UI 显示 `/health` 路由 | 通过 | ✅ |
| A5 | `uv run pytest -v` | 11 passed | 全部通过 | ✅ |

### 数据库一致性

- 7 张表全部按 DATA-MODEL.md 创建：`stocks / daily_bars / signals / pullbacks / market_indices / system_logs / journal_entries`
- Alembic upgrade/downgrade 循环验证通过
- 字段名、类型、索引、唯一约束与 ORM Schema 逐字段对比一致（pytest parametrize 覆盖）

### 发现的问题

无。

### 变更内容摘要（供 project-commiter 发版时提取）

- 新增后端骨架：FastAPI + SQLAlchemy 2.0 + Alembic
- 新增 `/health` 探活接口
- 初始数据库 schema（7 张表）
- 项目级 git 仓库初始化

---

## F000-c Docker Compose + Polygon Client（验收通过）

**日期**：2026-04-17
**验收者**：用户本人
**Sprint Contract**：docs/开发/sprint-contracts/F000-c-contract.md
**Commit**：`0764e10`

### 验收清单执行结果

| # | 检查项 | 期待 | 实际 | 状态 |
|---|-------|------|------|------|
| A1 | `docker compose build` | 无错误 | 前后端镜像构建成功 | ✅ |
| A2 | `docker compose up -d` | 两容器 running | backend + frontend 均 Up | ✅ |
| A3 | `curl localhost:8080/` | 200 SPA | 200 + React SPA HTML | ✅ |
| A4 | `curl localhost:8080/api/health` | `{"status":"ok"}` | 200 + 正确响应（nginx 反代生效） | ✅ |
| A5 | SPA fallback `/journal` | 200 | 200（nginx try_files 生效） | ✅ |
| A6 | 浏览器访问 `localhost:8080` 三路由 | 均可渲染 | 通过 | ✅ |
| A7 | backend 自动跑 alembic upgrade | prod.db 生成 | 日志显示 `001_initial` 迁移成功 | ✅ |
| A8 | Volume 持久化 | restart 后 prod.db 保留 | `/app/data/prod.db` 保留 | ✅ |
| A9 | `cd backend && uv run pytest` | 全部通过 | 19/19（polygon 8 + 原 11 回归）| ✅ |
| A10 | `.env` 未被 git tracked | `git check-ignore .env` 返回 0 | 通过 | ✅ |

### 技术决策记录

- D013：Polygon 客户端采用 `massive` 2.5.0（官方改名后包）而非 `polygon-api-client`
- D014：Rate limit 采用线程安全 token bucket（容量 5 / 60s 窗口 / 12s 补 1 token）

### 发现的问题

无。

### 变更内容摘要（供 project-commiter 发版时提取）

- 新增 docker-compose 一键启动能力（前端 8080:80，后端内部网络）
- 新增 nginx 反向代理（/api/* → backend:8000，SPA fallback）
- 新增 `PolygonClient` 封装（`massive` 包 + 自建 token-bucket rate limiter）
- 新增 Dockerfile（backend: python:3.12-slim + uv；frontend: node:20 构建 → nginx:alpine 托管）
- 新增 volume 挂载保障 SQLite 数据持久化

---

## F001-a Backend Watchlist + Stock Search API（验收通过）

**日期**：2026-04-17
**验收者**：用户本人
**Sprint Contract**：docs/开发/sprint-contracts/F001-a-contract.md

### 验收清单执行结果

| # | 检查项 | 期待 | 实际 | 状态 |
|---|-------|------|------|------|
| A1 | `pytest` 全量 | ≥36 通过 | 38 passed（health 1 + polygon 8 + schema 10 + watchlist 19）| ✅ |
| A2 | `GET /api/watchlist` 空库 | `{"data":[],"message":"success"}` | 通过 | ✅ |
| A3 | `POST /api/watchlist {ticker:AAPL}` 实货 Polygon | 201 + Apple Inc./XNAS + dataStatus:loading | 通过 | ✅ |
| A4 | `GET /api/stocks/search?q=AA` 实货 Polygon | 10 条结果 | Alcoa + 9 条 ETF/股票 | ✅ |
| A5 | `GET /api/stocks/search` 缺 q | 统一错误格式 VALIDATION_ERROR | 通过 | ✅ |
| A6 | 响应字段驼峰 | addedAt / dataStatus / latestSignal | 通过 | ✅ |
| A7 | DB 字段蛇形 | added_at / is_active | 通过（与 DATA-MODEL.md 一致） | ✅ |
| A8 | 统一错误格式 | `{"error":{"code","message"}}` + HTTP code | 通过 | ✅ |

### 技术决策记录

- D015：nginx `proxy_pass` 不带末尾斜杠（保留 `/api/` 前缀）
- D016：Polygon `list_tickers` 用 `itertools.islice` 截取首页，避免 SDK 自动翻页绕过 token bucket

### 发现的问题（均已修复）

1. 容器跑旧镜像 → `docker compose up -d --build backend` 解决（运维动作，非代码）
2. nginx proxy_pass 带末尾斜杠剥掉 `/api/` 前缀 → D015 修复
3. Polygon SDK auto-pagination 触发 429 → D016 修复

### 变更内容摘要

- 新增后端 4 端点闭环（GET/POST/DELETE /api/watchlist + GET /api/stocks/search）
- 新增 Repository / Service / Schemas / Router 分层（为后续 feature 奠定模式）
- 新增统一错误响应 handler（APIError + RequestValidationError）
- 新增测试 fixture 基座（in-memory SQLite + FakePolygon dependency override）
- 修复 nginx `/api/*` 反代前缀保留
- 修复 Polygon SDK 翻页越权 rate limit
