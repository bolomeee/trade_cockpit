# v1.0 验收记录

> 项目：MA150 Tracker | 起始日期：2026-04-17

---

## F000-a 后端脚手架 + 数据库基座

**验收日期**：2026-04-17
**Sprint Contract**：[docs/开发/sprint-contracts/F000-a-contract.md](../开发/sprint-contracts/F000-a-contract.md)
**commit**：`10a1e95`
**结论**：✅ 通过

### 验收执行记录

| # | 操作 | 期望 | 实际 | 状态 |
|---|------|------|------|------|
| A1 | `uv sync` 安装依赖 | 无错误 | 通过 | ✅ |
| A2 | `uv run uvicorn app.main:app --reload` 启动 | 控制台显示 running、无 ERROR | 通过 | ✅ |
| A3 | `curl /health` | `{"status":"ok"}` | 通过 | ✅ |
| A4 | 浏览器 `/docs` | Swagger UI 显示 `/health` 路由 | 通过 | ✅ |
| A5 | `uv run pytest -v` | 11 passed | 全部通过 | ✅ |

### 数据库一致性

- 7 张表全部按 DATA-MODEL.md 创建：`stocks / daily_bars / signals / pullbacks / market_indices / system_logs / journal_entries`
- Alembic upgrade/downgrade 循环验证通过
- 字段名、类型、索引、唯一约束与 ORM Schema 逐字段对比一致（pytest parametrize 覆盖）

### 发现的问题

无。

### 变更内容摘要（供 project-commiter 发版时提取）

- 新增后端骨架：FastAPI + SQLAlchemy 2.0 + Alembic
- 新增 `/health` 探活接口
- 初始数据库 schema（7 张表）
- 项目级 git 仓库初始化

---

## F000-c Docker Compose + Polygon Client（验收通过）

**日期**：2026-04-17
**验收者**：用户本人
**Sprint Contract**：docs/开发/sprint-contracts/F000-c-contract.md
**Commit**：`0764e10`

### 验收清单执行结果

| # | 检查项 | 期待 | 实际 | 状态 |
|---|-------|------|------|------|
| A1 | `docker compose build` | 无错误 | 前后端镜像构建成功 | ✅ |
| A2 | `docker compose up -d` | 两容器 running | backend + frontend 均 Up | ✅ |
| A3 | `curl localhost:8080/` | 200 SPA | 200 + React SPA HTML | ✅ |
| A4 | `curl localhost:8080/api/health` | `{"status":"ok"}` | 200 + 正确响应（nginx 反代生效） | ✅ |
| A5 | SPA fallback `/journal` | 200 | 200（nginx try_files 生效） | ✅ |
| A6 | 浏览器访问 `localhost:8080` 三路由 | 均可渲染 | 通过 | ✅ |
| A7 | backend 自动跑 alembic upgrade | prod.db 生成 | 日志显示 `001_initial` 迁移成功 | ✅ |
| A8 | Volume 持久化 | restart 后 prod.db 保留 | `/app/data/prod.db` 保留 | ✅ |
| A9 | `cd backend && uv run pytest` | 全部通过 | 19/19（polygon 8 + 原 11 回归）| ✅ |
| A10 | `.env` 未被 git tracked | `git check-ignore .env` 返回 0 | 通过 | ✅ |

### 技术决策记录

- D013：Polygon 客户端采用 `massive` 2.5.0（官方改名后包）而非 `polygon-api-client`
- D014：Rate limit 采用线程安全 token bucket（容量 5 / 60s 窗口 / 12s 补 1 token）

### 发现的问题

无。

### 变更内容摘要（供 project-commiter 发版时提取）

- 新增 docker-compose 一键启动能力（前端 8080:80，后端内部网络）
- 新增 nginx 反向代理（/api/* → backend:8000，SPA fallback）
- 新增 `PolygonClient` 封装（`massive` 包 + 自建 token-bucket rate limiter）
- 新增 Dockerfile（backend: python:3.12-slim + uv；frontend: node:20 构建 → nginx:alpine 托管）
- 新增 volume 挂载保障 SQLite 数据持久化

---

## F001-a Backend Watchlist + Stock Search API（验收通过）

**日期**：2026-04-17
**验收者**：用户本人
**Sprint Contract**：docs/开发/sprint-contracts/F001-a-contract.md

### 验收清单执行结果

| # | 检查项 | 期待 | 实际 | 状态 |
|---|-------|------|------|------|
| A1 | `pytest` 全量 | ≥36 通过 | 38 passed（health 1 + polygon 8 + schema 10 + watchlist 19）| ✅ |
| A2 | `GET /api/watchlist` 空库 | `{"data":[],"message":"success"}` | 通过 | ✅ |
| A3 | `POST /api/watchlist {ticker:AAPL}` 实货 Polygon | 201 + Apple Inc./XNAS + dataStatus:loading | 通过 | ✅ |
| A4 | `GET /api/stocks/search?q=AA` 实货 Polygon | 10 条结果 | Alcoa + 9 条 ETF/股票 | ✅ |
| A5 | `GET /api/stocks/search` 缺 q | 统一错误格式 VALIDATION_ERROR | 通过 | ✅ |
| A6 | 响应字段驼峰 | addedAt / dataStatus / latestSignal | 通过 | ✅ |
| A7 | DB 字段蛇形 | added_at / is_active | 通过（与 DATA-MODEL.md 一致） | ✅ |
| A8 | 统一错误格式 | `{"error":{"code","message"}}` + HTTP code | 通过 | ✅ |

### 技术决策记录

- D015：nginx `proxy_pass` 不带末尾斜杠（保留 `/api/` 前缀）
- D016：Polygon `list_tickers` 用 `itertools.islice` 截取首页，避免 SDK 自动翻页绕过 token bucket

### 发现的问题（均已修复）

1. 容器跑旧镜像 → `docker compose up -d --build backend` 解决（运维动作，非代码）
2. nginx proxy_pass 带末尾斜杠剥掉 `/api/` 前缀 → D015 修复
3. Polygon SDK auto-pagination 触发 429 → D016 修复

### 变更内容摘要

- 新增后端 4 端点闭环（GET/POST/DELETE /api/watchlist + GET /api/stocks/search）
- 新增 Repository / Service / Schemas / Router 分层（为后续 feature 奠定模式）
- 新增统一错误响应 handler（APIError + RequestValidationError）
- 新增测试 fixture 基座（in-memory SQLite + FakePolygon dependency override）
- 修复 nginx `/api/*` 反代前缀保留
- 修复 Polygon SDK 翻页越权 rate limit

---

## F001-b Frontend Watchlist 读取展示（验收通过）

**日期**：2026-04-17
**验收者**：用户本人
**Sprint Contract**：docs/开发/sprint-contracts/F001-b-contract.md
**Commit**：`c49b2ba`（+ `49c8e16` config.py 修复）

### 验收清单执行结果

| # | 检查项 | 期待 | 实际 | 状态 |
|---|-------|------|------|------|
| A1 | Ready 状态：AAPL 卡片 + INSUFFICIENT badge | ticker/name/badge 显示正确 | AAPL / Apple Inc. / INSUFFICIENT | ✅ |
| A2 | Empty 状态：watchlist 为空 | "还没有自选股，从右侧 Add Stock 开始吧" | 用户截图确认 | ✅ |
| A3 | Error 状态：后端停止时 | "数据加载失败" + 重试按钮（红色文字） | preview 验证通过 | ✅ |
| A4 | Loading 状态：首屏加载中 | 4 个 Skeleton 占位卡片 | preview 截图确认 | ✅ |
| A5 | pnpm build 零 TS 错误 | build 输出 0 errors | 通过 | ✅ |
| A6 | SignalBadge 只用 token 颜色 | 无硬编码 hex | 代码审查通过 | ✅ |

### 技术决策记录

- D017：前端数据获取统一使用 `@tanstack/react-query` v5
- D018：Vite dev server 添加 `/api` → `localhost:8000` 代理

### 发现的问题（均已修复）

1. `backend/app/config.py` 用相对 CWD 查找 `.env`，`cd backend` 启动时找不到 → 改用 `Path(__file__)` 绝对路径解析（commit `49c8e16`）
2. `button.tsx` ESLint `react-refresh/only-export-components` → 预先存在（F000-b 遗留），不影响 F001-b，已知可接受

### 变更内容摘要

- 新增 API 层：`lib/api/client.ts`（fetch 封装）+ `lib/api/watchlist.ts`
- 新增 TypeScript 类型：`types/watchlist.ts`（WatchlistItem / LatestSignal / DataStatus / SignalType）
- 新增 Dashboard 组件：`SignalBadge` / `SignalCard` / `SignalBoard`（信号优先级排序）
- 新增通用组件：`EmptyState` / `ErrorState`
- 更新 `Dashboard.tsx`：接入 `useQuery`，四态切换
- 更新 `main.tsx`：接入 `QueryClientProvider`
- 新增 `vite.config.ts` dev proxy（开发环境连接后端）
- 修复 API-CONTRACT.md 文档：补充 `dataStatus` 字段说明

---

## F004 信号总览 SignalBoard（验收通过）

**日期**：2026-04-17
**验收者**：用户本人
**Sprint Contract**：docs/开发/sprint-contracts/F004-contract.md
**Commit**：`b2bf52b`

### 验收清单执行结果

| # | 检查项 | 期待 | 实际 | 状态 |
|---|-------|------|------|------|
| A1 | SignalBoard 数据源 | `GET /api/signals` 成功返回并按优先级排序 | AAPL(BUY_ZONE) 置顶，CMC/SMCI(NEUTRAL) 在后 | ✅ |
| A2 | SignalCard 展示 | ticker / 公司名 / closePrice / distance% / Badge | 完整显示（$263.40 / +0.4% MA150） | ✅ |
| A3 | 信号颜色 | BUY_ZONE/NEUTRAL 分别用 token 颜色 | 符合 design-spec | ✅ |
| A4 | 点击打开 Modal | Modal 显示 ticker + Badge + 4 指标；URL 不变 | 通过（portaled dialog 渲染 4 metrics） | ✅ |
| A5 | Modal 关闭 | ESC / backdrop / X 均可关闭 | 通过 | ✅ |
| A6 | 删除按钮 | 不触发 Modal（stopPropagation） | 通过（F001-c 实现保留） | ✅ |
| A7 | 移动端布局 | <768px 单列 | `gridTemplateColumns: 145px` | ✅ |
| A8 | Backend 回归 | 108/108 pytest 通过 | 通过 | ✅ |
| A9 | 前端 TSC | `tsc -b` 无错误 | 通过 | ✅ |

### 技术决策记录

- 无新增。StockDetailModal 复用 shadcn `dialog.tsx`；Modal 状态由 Dashboard 本地 `useState` 管理（component-plan.md 约定）

### 发现的问题

无。

### 已知偏离

- 预计 6 文件，实际 8 文件：额外 2 处为 `AddStockCard` / `useRefreshStatus` 的单行 `invalidateQueries(['signals'])` 同步（数据源切换的必要联动），已在 Generator 阶段告知并获批

### 变更内容摘要（供 project-commiter 发版时提取）

- 新增 `types/signal.ts`（`SignalBoardItem` 对齐 API-CONTRACT）
- 新增 `lib/api/signals.ts`（`getSignals()`）
- 新增 `StockDetailModal.tsx`（最小壳：Header + 4 指标，F005 将填充 Chart/Pullback/Fundamentals）
- 重构 `SignalBoard` / `SignalCard` 消费 `SignalBoardItem`；SignalCard 补 closePrice 展示
- `Dashboard` 数据源切换为 `/api/signals`，新增 modal state
- `AddStockCard` / `useRefreshStatus` 同步 invalidate `['signals']`


---

## F005 个股 150MA 详情 Modal（2026-04-17）

**子 Sprint**：F005-a（backend）+ F005-b（Header/Pullback/Fundamentals）+ F005-c（PriceChart）

### 验收清单

| # | 标准 | 期望 | 实际 | 结果 |
|---|------|------|------|------|
| A1 | K 线图 + MA150 | Candles + 蓝色 MA150 叠加 | lightweight-charts v5，MA 自 150 日起 | ✅ |
| A2 | Pullback 标记 | K 线下方标记回踩日期 | createSeriesMarkers v5 primitive，arrowUp + --color-signal-buyzone | ✅ |
| A3 | 斜率 + 距离% | Slope ↑/↓，Distance % | Header 4 指标正确展示 | ✅ |
| A4 | 回踩记录表 | Date/Distance/10D/20D/30D，null 显示 "—" | 4 行，正负色符合 token | ✅ |
| A5 | 基本面 | PE/PS/PEG/FCF + Mock Badge | 2×2 Grid，Badge 常驻 | ✅ |
| A6 | Modal 打开/关闭 | 点 SignalCard 开，ESC/X/Backdrop 关，URL 不变 | 通过 | ✅ |
| A7 | Chart 生命周期 | 切换股票 chart 正确销毁重建，无泄漏 | cleanup 调用 chart.remove()，re-render 7 canvases | ✅ |
| A8 | 四种状态 | 正常/加载(Skeleton)/错误(Retry)/短周期(<150d 无 MA 线) | 三态已实现，短周期后端 ma150=[] 前端 if guard | ✅ |
| A9 | tokens 合规 | 颜色/尺寸用 CSS 变量，无硬编码 | getComputedStyle 运行时读取 tokens.css | ✅ |
| A10 | 后端回归 | pytest 全绿 | 118/118 | ✅ |
| A11 | 前端 build | tsc + vite build 通过 | 通过 | ✅ |

### 技术决策记录

- D025 引入 lightweight-charts v5.1.0；v5 API（`addSeries(CandlestickSeries)` / `createSeriesMarkers`）经 context7 文档验证
- tokens 运行时注入策略（getComputedStyle）——让 chart 主题与 CSS 变量联动，避免双维护

### 发现的问题

- Pydantic `alias_generator=to_camel` 将 `return_10d` 错序列化为 `return10D`（F005-b 验证期发现）→ 已在 schemas 显式 `alias="return10d"` 修复，并强化测试 fixture 确保 pullback 至少一条以暴露问题
- F005-a 的 `test_pullbacks_sorted_desc_with_nullable_returns` 原 fixture 产 0 行，断言被 `if rows` 跳过 → 改为 proven fixture 并 `assert rows`

### 已知偏离

- F005 初估 1 个 Sprint，实际拆成 3（F005-a/b/c）—— 6 文件上限拆分，符合规则
- Chart 主题单套（浅色），深色模式留给未来 feature
- Pullback marker hover 展开详情（P2）未实现，本次只画三角形

### 变更内容摘要

- **Backend（F005-a）**：新增 `stock_detail` schemas / `PullbackRepository` / `StockDetailService` / 3 routes（`/chart` `/pullbacks` `/fundamentals`）+ 10 tests
- **Frontend（F005-b）**：新建 `stock-detail/` 目录；`StockDetailModal` + `StockDetailHeader` + `PullbackHistoryCard` + `FundamentalsCard`；删除旧 `dashboard/StockDetailModal`
- **Frontend（F005-c）**：引入 `lightweight-charts@5.1.0`；新建 `PriceChart.tsx`；Modal 接入 chart 查询 + 三态处理
