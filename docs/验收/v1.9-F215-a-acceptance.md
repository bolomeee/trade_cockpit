# 验收记录：F215-a — Risk cap (1.5%→1.25%) + EMA 10/21

> 日期：2026-05-12 | 验收人：用户 | 结论：✅ 通过

---

## 功能范围

**Feature**：F215-a（F215 Cockpit Phase A 的第一段）  
**Sprint Contract**：docs/开发/sprint-contracts/F215-a-contract.md  
**涉及文件**：6 个（达到 6-file 上限）

| 文件 | 改动 |
|------|------|
| `backend/app/services/cockpit/cockpit_params.py` | RISK_ON 1.5→1.25；新增 CHART.DEFAULT_EMAS=[10,21] |
| `backend/app/services/cockpit/chart_service.py` | 新增 `_compute_ema_series()`；`get_chart()` 返回 emas |
| `backend/app/schemas/cockpit/chart.py` | CockpitChartData 新增 emas 字段 |
| `frontend/src/cockpit/lib/api/cockpitChartApi.ts` | CockpitChartData type 新增 emas |
| `frontend/src/cockpit/widgets/CockpitChartWidget.tsx` | 渲染 EMA10/EMA21 虚线（LineStyle.Dashed） |
| `frontend/src/cockpit/widgets/MarketRegimeWidget.tsx` | toFixed(1)→toFixed(2)（精度对齐 1.25%） |

---

## 自动化探针结果

| # | 验证项 | 结果 |
|---|--------|------|
| A1 | `GET /api/cockpit/chart/NVDA` 返回 emas 字段 | ✅ emas.10=241点，emas.21=230点 |
| A2 | emas 键为 ["10","21"] | ✅ |
| A3 | 无 console.error | ✅ |
| A4 | 后端 test_f215a.py 12/12 通过 | ✅ |
| A5 | 前端 CockpitChartWidget S9 通过 | ✅ |
| A6 | 前端 MarketRegimeWidget S9 toFixed(2) 通过 | ✅ |

---

## 手动验收确认

用户于 2026-05-12 确认以下内容合格：

- [x] EMA10/EMA21 在 Chart 图例中以虚线（`-- EMA10`、`-- EMA21`）展示，与 SMA 实线视觉区分清晰
- [x] 切换 ticker 后 EMA 线跟随更新，无报错
- [x] MarketRegimeWidget 精度显示正确（2 位小数）
- [x] Cockpit 整体无功能回归

---

## 已知情况（不阻塞验收）

**Risk/Trade 显示 1.50%（而非 1.25%）**：  
原因：DB 中的 market_regime_snapshot 系本次代码修改前写入，下次 `MarketRegimeService.compute_and_store()` 执行后将自动写入 1.25%。代码层面已修正（后端测试验证），无需 DB migration。见 D085。

---

## 测试套件状态

**后端**（`uv run pytest`）：959 passed，6 failed（全部为预存在失败，与 F215-a 无关）  
- `test_regime_f201a/b`：INDEX_ETFS 计数断言未随 VXX 添加（commit `52cdc4d`）而更新，预存在  
- AI schema tests（F209/F211）：AI 服务依赖，预存在  
- `test_fmp_client`：FMP 数据问题，预存在

**前端**（`pnpm test`）：274 passed，30 failed（全部预存在，与 F215-a 无关）  
- AiNewsSummaryBar、SetupMonitorWidget AI 相关、TopNav Tooltip、e2e/f211-smoke 等

---

## Consistency Check 摘要

| 检查 | 结论 |
|------|------|
| C1 父 feature 完成性 | ✅ 无违例（F215 parent=in_progress，F215-b=design_needed，合理） |
| C2 dependencies | ✅ 无违例 |
| C5 sub_sprints ↔ 合约 | ⚠️ F215-b 无合约文件（design_needed 预期，不阻塞） |
| C7 phase 转换 | ✅ 已在本次验收中补记完整 iteration_history |

---

## 结论

**F215-a 验收通过**，phase 更新为 done。F215-b（Volume Accumulation）仍为 design_needed，将在独立 session 中规划。
