# 验收记录：F206-c2 PendingOrdersWidget

**日期**：2026-04-26
**Feature**：F206-c2 — PendingOrdersWidget + PendingOrderFormDialog
**版本**：v1.9（F206 最终 sprint）
**验收人**：用户
**结论**：✅ 通过

---

## 技术测试（验收前已全部通过）

- 单元/集成测试：212/212 ✅
- lint：0 新增 warning ✅
- build：tsc + vite ✅
- S1–S20 全部通过（含 API client、distance 颜色、dirty-fields PATCH、Registry 注册）

## 视觉确认

| # | 检查项 | 结果 |
|---|-------|------|
| V1 | Widget 标题 "Pending Orders" | ✅ |
| V2 | Active/All 切换按钮，Active 默认高亮 | ✅ |
| V3 | "+ New Order" 按钮 | ✅ |
| V4 | 空态 "暂无 pending order" | ✅ |
| V5 | 与 Positions widget 同行，右半部分 | ✅ |

## 业务逻辑确认

| # | 场景 | 结果 |
|---|------|------|
| B1 | + New Order → 弹对话框 → 提交有效数据 → 行出现，Exp 列显示 "—" | ✅ |
| B2 | 空提交 + 交叉校验（entry ≤ stop）错误提示 | ✅ |
| B3 | Distance 3 档颜色（绝对值阈值） | ✅ |
| B4 | Edit → 预填字段 → 只改 stop → PATCH 仅含脏字段 | ✅ |
| B5 | Triggered → AlertDialog 确认 → 行消失 → toast 含 "TRIGGERED"+"Positions" → All 视图可见 → 无 [Triggered]/[Cancel] 按钮 | ✅ |
| B6 | Cancel 直接 PATCH 无弹窗；✕ → AlertDialog → 确认删除 | ✅ |
| E1 | Active/All 切换正确过滤 | ✅ |

## 已知问题

无。

---

## F206 完整回顾

| Sprint | 内容 | 验收时间 |
|--------|------|---------|
| F206-a | 数据库 migration（positions + pending_orders） + 后端 schema | 2026-04-26 |
| F206-b1 | positions API（GET/POST/PATCH/DELETE） | 2026-04-26 |
| F206-b2 | pending_orders API（GET/POST/PATCH/DELETE） | 2026-04-26 |
| F206-c1 | PositionListWidget + PositionFormDialog | 2026-04-26 |
| F206-c2 | PendingOrdersWidget + PendingOrderFormDialog | 2026-04-26 |

F206 全部 sprint 验收通过，phase → done。
