# 验收记录 — F218-d3a

**项目**：MA150 Tracker → Workbench → Cockpit  
**Feature**：F218-d3a — T2 Margin Expansion 数据层（income-statement client + key_metrics 表 + pool_cache 集成）  
**Iteration**：v2.4  
**验收日期**：2026-05-19  
**验收结论**：✅ 通过  
**Sprint Contract**：`docs/开发/sprint-contracts/F218-d3a-contract.md`

---

## 技术测试结果

| 测试类 | 数量 | 结果 |
|--------|------|------|
| TestFmpClientIncomeStatementQuarterly | 2 | ✅ |
| TestKeyMetricsRepository | 3 | ✅ |
| TestComputeKeyMetricsRow（参数化展开） | 5 | ✅ |
| TestPoolCacheKeyMetricsIntegration | 2 | ✅ |
| d1+d2 既有测试（无回归） | 28 | ✅ |
| 全量后端回归 | 1154 passed | ✅ |
| Pre-existing failures | 9 | = d2 baseline，无新增 |

---

## Contract AC 确认

| # | 标准 | 状态 |
|---|------|------|
| AC1 | `get_income_statement_quarterly` params 含 `period=quarter & limit=8 & symbol` | ✅ |
| AC2 | HTTP 错误 fail-open 返 `[]`（与 D079 一致） | ✅ |
| AC3 | margin 浮点不四舍五入，DB Float 自然存储 | ✅ 可接受 |
| AC4 | revenue=0/None → 3 margin 全 None；grossProfit=None → 仅 gross_margin=None | ✅ |
| AC5 | 输出 dict 精确 7 keys，不含 fcf_margin / roic | ✅ 策略可接受 |
| AC6 | `upsert` happy path + 覆盖更新 | ✅ |
| AC7 | null-not-erase：gross_margin=0.75 保留；fcf_margin=0.18 写入 | ✅ 理解并确认 |
| AC8 | `get_recent_for_ticker` DESC 排序 + ticker 隔离 + 空返 [] | ✅ |
| AC9 | `_rebuild_key_metrics` 4 行写入；失败 ticker 不阻断 | ✅ |
| AC10 | `rebuild()` 端到端：cockpit_pool_cache 不受影响 + key_metrics 写入 + 双 system_log | ✅ |

---

## 设计边界确认

| 边界 | 结论 |
|------|------|
| `_detect_margin_expansion` 仍为占位（detector 留 d3b） | ✅ 确认 |
| fcf_margin / roic 列保持 NULL 直到 d6a | ✅ 确认 |
| `delete_for_tickers_not_in` 实装但不挂调用点 | ✅ 确认 |
| FMP rate limiter 复用既有 token bucket，不引入第二个 | ✅ 确认 |
| Alembic 023 迁移链正确（down_revision = 022） | ✅ round-trip 通过 |

---

## 实装文件清单

| 文件 | 改动 |
|------|------|
| `backend/app/external/fmp_client.py` | +1 常量 + 1 方法 `get_income_statement_quarterly` |
| `backend/app/models/stock_key_metrics_quarterly.py` | 新建，9 列 ORM |
| `backend/app/models/__init__.py` | +1 import + __all__ 注册 |
| `backend/alembic/versions/023_f218_d3a_stock_key_metrics_quarterly.py` | 新建，upgrade + downgrade |
| `backend/app/repositories/key_metrics_repository.py` | 新建，3 方法 |
| `backend/app/services/cockpit/pool_helpers.py` | +1 纯函数 `compute_key_metrics_row_from_income_statement` |
| `backend/app/services/cockpit/pool_cache_service.py` | +2 私有方法 + `rebuild()` 追加调用 |
| `backend/tests/test_f218_d3a_key_metrics.py` | 新建，10 方法 / 4 class |

---

## 下一步

**F218-d3b**：`_detect_margin_expansion` 占位 → 真实业务逻辑实装  
读取 `KeyMetricsRepository.get_recent_for_ticker(ticker, limit=4)` → 最近 2 季 vs 上年同期 → gross_margin 扩张 ≥ 200bp 触发
