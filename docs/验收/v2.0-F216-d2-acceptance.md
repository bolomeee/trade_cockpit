# 验收记录 — F216-d2 Weekly Stage 强制门禁接入 ready_signal

**日期**：2026-05-14
**Feature**：F216-d2（F216 Cockpit Phase B — Weekly Stage Layer 第 4 步 service gate）
**Sprint Contract**：docs/开发/sprint-contracts/F216-d2-contract.md
**验收结论**：✅ 通过

---

## 测试门禁结果（自动化，Evaluator 完成）

| 测试文件 | 结果 |
|---------|------|
| `test_setup_f216d2.py` | 28/28 passed（T1-T16：纯函数 + 集成 + row_to_dict + schema） |
| `test_setup_f202a.py` | 27/27 passed（s10/s11 fixture 加 weekly_stage=2 后语义不变） |
| `test_setup_f202b.py` | 全绿 |
| `test_setup_service_f215b.py` | 全绿 |
| 全量回归 | 1047 passed（+28 new），7 pre-existing failures（与本 sprint 无关） |

---

## 业务逻辑确认

| # | 验收项 | 结果 |
|---|-------|------|
| B1-B4 | 所有测试层级全绿（见上方） | ✅ |
| B5 | GET /api/cockpit/setup-monitor → items[] 含 `weeklyStage` 字段 | ✅ 用户确认 |
| B6 | ready_signal=true 标的数量减少（Stage != 2 被过滤） | ✅ 用户确认 |
| B7 | weeklyStage 值与 weekly_stage_snapshots 最新行一致 | ✅ 用户确认 |

---

## 文档审查

| 文档 | 改动 | 结果 |
|------|------|------|
| API-CONTRACT.md §setup-monitor | 加 `weeklyStage` 字段说明 + readySignal 描述 7→8 条 AND | ✅ |
| DATA-MODEL.md §SetupSnapshot | ready_signal 描述 7→8；业务规则段加 Weekly Stage 门禁 bullet (F216-d2/D093) | ✅ |
| DECISIONS.md | 追加 D093（含放弃了什么 3 项 + flag 回滚说明） | ✅ |

---

## 实现摘要

**7 文件**（NP-A 单 sprint，用户 2026-05-14 同意）：

| 文件 | 改动 |
|------|------|
| `backend/app/services/cockpit/setup_service.py` | `_compute_ready_signal` 加 weekly_stage 参数 + stage gate；`__init__` 加 weekly_stage_repo；`compute_and_store_all` 批量 join stage_map（含短数据分支）；`_row_to_dict` 加 weeklyStage |
| `backend/app/schemas/cockpit/setup.py` | `SetupItemResponse` 加 `weekly_stage: int \| None = None` |
| `backend/app/services/cockpit/cockpit_params.py` | §2 加 `READY_REQUIRE_STAGE2: bool = True` |
| `backend/tests/test_setup_f216d2.py` | 新建，28 测试全绿 |
| `docs/系统设计/API-CONTRACT.md` | §setup-monitor 同步 |
| `docs/系统设计/DATA-MODEL.md` | §SetupSnapshot 同步 |
| `docs/系统设计/DECISIONS.md` | 追加 D093 |

**Git commits**：
- `0fbe6b7` wip(F216-d2): ready_signal 8th AND gate + stage flag + pure tests
- `ed16eb8` wip(F216-d2): compute_and_store_all join + row_to_dict + integration tests
- `ca665a6` feat(F216-d2): weekly_stage as 8th ready_signal gate（docs sync）

---

## 生产环境注意事项

- 首次部署前需手动跑 `WeeklyStageService.compute_and_store_all()` 否则所有 ticker weekly_stage=None → ready=False
- 或临时 `READY_REQUIRE_STAGE2=False` 等 cron 跑完再开启（flag toggle 无需 migration）

---

## 下一步

- **F216-d3**：前端 WS 列（`setupMonitorApi.ts` + `SetupMonitorWidget.tsx`）
- **F216-e**：refresh_job cron 编排（regime → weekly_stage → setup）
