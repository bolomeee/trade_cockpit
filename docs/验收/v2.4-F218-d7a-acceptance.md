# F218-d7a 验收记录

**项目**：MA150 Tracker → Workbench
**Feature**：F218-d7a — Repricing Trigger cron 注册 + router + 2 endpoint
**日期**：2026-05-20
**验收者**：Claude Code (Sonnet 4.6)
**Sprint Contract**：docs/开发/sprint-contracts/F218-d7a-contract.md

> 本 sub-sprint 为纯后端功能（无前端 UI），无视觉确认项。
> 验收聚焦：API 行为正确性、cron 注册、evidence 字段转换、UTC 时间格式。

---

## Consistency Check 入口扫描

```
__CONSISTENCY_CHECK_RESULT__: severe=0 medium=0 minor=0 exit=0
```

（由 feature-dev Evaluator 阶段调用 consistency-check interactive 完成，直接沿用。）

---

## 业务逻辑验收（acceptance_criteria #8、#9、#10）

| # | 验收标准 | 来源 AC | 结果 | 说明 |
|---|---------|--------|------|------|
| B1 | `start_scheduler` 注册 `cockpit_repricing_triggers`；trigger hour=22 / minute=40 / weekdays；timezone=UTC | AC#8 | ✅ 通过 | 11 个 jobs，22:40 UTC mon-fri，无时段冲突 |
| B2 | OpenAPI 含 `/api/cockpit/repricing-triggers/{ticker}` 和 `/api/cockpit/repricing-triggers` 两条路径 | AC#9/10 | ✅ 通过 | app.openapi() 已包含两路径 |
| B3 | evidence dict 5 类 snake→camel 转换（T1/T2/T3/T4/T5 全覆盖） | AC#9/10 | ✅ 通过 | `epsYoyGrowth` / `expansionBp` / `matchCount` / `rsPercentile` / `netDebtTrend` 均正确 |
| B4 | Pydantic schema 6 类全部可 import，TriggerType Literal 含 5 类枚举 | — | ✅ 通过 | — |
| B5a | GET `/api/cockpit/repricing-triggers/NVDA` → 200；3 triggers；camelCase fields；evidence keys camelCase；`computedAt` 含 `+00:00` | AC#9 | ✅ 通过 | `computedAt=2026-05-15T22:40:00+00:00` |
| B5b | GET `/api/cockpit/repricing-triggers` → 200；`totalCount=3`；`computedAt` 含 UTC offset | AC#10 | ✅ 通过 | — |
| B5c | `?triggerType=EARNINGS_ACCEL` 过滤 → `totalCount=1` | AC#10 | ✅ 通过 | — |
| B5d | 非法 ticker / `?triggerType=INVALID` / `?limit=0` → 422 | AC#9/10 | ✅ 通过 | — |

---

## Bug 发现与修复

| # | 问题 | 发现时机 | 修复 | 状态 |
|---|------|---------|------|------|
| BUG-1 | `computed_at` 从 SQLite 读回后 tzinfo=None，`.isoformat()` 裸输出无 UTC 后缀 | acceptance 静态验证 | `_row_to_item` 和市场 endpoint 补 `.replace(tzinfo=timezone.utc)`，commit: fix(F218-d7a) | ✅ 已修复 |

---

## 自动化测试门禁（已通过）

| 测试集 | 数量 | 结果 |
|--------|------|------|
| F218-d7a 新测试（cron + router） | 11 | ✅ 11/11 |
| d1-d6b 既有测试 | 109 | ✅ 109/109 |
| 全量后端回归 | 1234 passed | ✅ 9 pre-existing，未新增 |

---

## 范围确认（不在 d7a 范围）

- ❌ 前端 widget / DecisionPanel chip → d7b
- ❌ T1-T5 detector 逻辑 → d1-d6b 收工
- ❌ 历史回测 NVDA/TSLA/META → acceptance 收官统一做

---

## 结论

**✅ F218-d7a 验收通过。**

发现并修复 1 项 Bug（computedAt UTC 格式）。全量测试无新增失败。
下一步：协商 F218-d7b Sprint Contract（前端 widget）。
