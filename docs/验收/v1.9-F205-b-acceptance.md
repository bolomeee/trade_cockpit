# 验收记录：F205-b FMP 增量 + Pool 计算 Helpers

> 版本：v1.9（Cockpit P1）
> 验收日期：2026-04-27
> 验收结论：✅ 通过
> Sprint Contract：docs/开发/sprint-contracts/F205-b-contract.md

---

## 交付内容

| # | 文件 | 类型 | 说明 |
|---|------|------|------|
| 1 | `backend/app/external/fmp_client.py` | 修改 | 新增 `get_financial_growth(symbol) -> dict \| None` |
| 2 | `backend/app/services/cockpit/pool_helpers.py` | 新建 | 5 个纯函数（pool funnel 计算层） |
| 3 | `backend/tests/test_pool_helpers_f205b.py` | 新建 | 31 条单元测试 |
| 4 | `backend/tests/test_fmp_client.py` | 修改 | 新增 5 条 get_financial_growth 测试 |
| 附加 | `docs/系统设计/DECISIONS.md` | 追加 D079 | RS mid-rank 算法 / fail-open / 技术债 |

---

## 测试门禁（Evaluator 结果）

| 测试集 | 结果 |
|--------|------|
| `test_pool_helpers_f205b.py` | ✅ 31/31 |
| `test_fmp_client.py` | ✅ 54/54 |
| 全量回归 `pytest tests/` | ✅ 794 passed（758 + 36 新增，无新失败） |
| F202-a 回归（setup + scanner） | ✅ 52/52 |
| `pool_helpers.py` 纯净度（AST 静态） | ✅ 无 app.*/logging/SQLAlchemy/httpx |
| `setup_service.py` 未被修改 | ✅ git diff 为空 |

---

## 验收清单确认

| # | 行为 | 结论 |
|---|------|------|
| B1 | FMP 空数组 → get_financial_growth 返回 None | ✅ |
| B2 | FMP HTTP 500/404 → 返回 None，不报错 | ✅ |
| B3 | FMP 429 → 自动退避重试，成功后返回数据 | ✅ |
| B4 | 序列不足 250 条 → compute_return_ratio_250d 返回 None | ✅ |
| B5 | SPY flat（涨跌幅≈0） → 返回 None | ✅ |
| B6 | RS percentile mid-rank：3 不同值 → 16.67/50.0/83.33 | ✅ |
| B7 | RS percentile ties：33.33/33.33/83.33 | ✅ |
| B8 | RS percentile：None ratio → 视为底部，不报错 | ✅ |
| B9 | revenueGrowth=0.0202 → extract 返回 2.02 | ✅ |
| B10 | "N/A"/None/字段缺失 → None（fail-open） | ✅ |
| B11 | passes_fundamental_sanity(None, 10.0) → True（fail-open D079） | ✅ |
| B12 | distance_to_50ma_pct 正常计算 | ✅ |
| B13 | pool_helpers.py 纯函数，无 IO 依赖 | ✅ |

---

## 关键决策确认（用户已确认）

- **RS percentile 使用 mid-rank**：`compute_rs_percentile_map` 采用 `(below + 0.5·ties)/n·100`
- **setup_service 保持不变**：`_percentile_rank` 仍用 strictly-below，双实现为已知技术债（D079）
- **fail-open 策略**：FMP 数据缺失时 `passes_fundamental_sanity` 返回 True，不淘汰 ticker

---

## 下一步

F205-c：PoolService + `GET /api/cockpit/pool` endpoint
- 将消费本 sprint 的 `pool_helpers` + `get_financial_growth`
- 负责编排、缓存（24h TTL）、漏斗组装、错误隔离日志
