# 最终验收记录：F207-a — ActionList 后端 (rule engine + endpoint)

**项目**：MA150 Tracker → Cockpit
**Feature**：F207-a Daily Action List 后端 (v1.9 Cockpit P1 子 sprint)
**日期**：2026-04-27
**Sprint Contract**：docs/开发/sprint-contracts/F207-a-contract.md
**验收路径**：A — 信任测试门禁（纯后端 sprint，无 UI）

---

## 验收结论

✅ **通过**。F207-a 是纯后端 sprint（rule engine + 1 个 endpoint，无 UI），按既往规则纯后端 sprint 走路径 A：以 Evaluator 测试门禁为准，不做视觉确认（视觉部分留 F207-b）。

---

## 测试门禁结果（Evaluator 已通过）

| 测试集 | 数量 | 结果 | 命令 |
|---|---|---|---|
| 单元（test_action_service.py） | 16 | ✅ 全通过 | `pytest backend/tests/test_action_service.py -v` |
| 集成（test_actions_router.py） | 6 | ✅ 全通过 | `pytest backend/tests/test_actions_router.py -v` |
| 全量回归 | 744 | ✅ 0 回归（基线 722，本 sprint +22） | `pytest backend/` |

覆盖：S1-S20 的可测试完成标准 20 条全部满足（详见 contract §3）。

---

## 业务逻辑覆盖（rule engine 优先级断言）

| 规则路径 | 用例 | 验证 |
|---|---|---|
| stop breached（exit）→ raise_stop "stop already breached" | U3 / I-集成 | ✅ |
| regime DEFENSIVE / RISK_OFF → tighten_stop（覆盖 stable） | U4 / U13 | ✅ |
| regime > earnings 优先级 | U7 | ✅ |
| earnings ≤ 2d → reduce_before_earnings | U6 | ✅ |
| R≥2.0 且 stop<entry → raise_stop（rule） | U5 | ✅ |
| rule=hold → no_action / stable_position | U2 | ✅ |
| pending_order setup BROKEN → cancel_order | U9 / I4 | ✅ |
| pending_order distance ≤3% → approaching_trigger | U8 / I3 | ✅ |
| pending_order distance >3% → 不展示 | U10 | ✅ |
| pending_order last_close=None → 不抛错不入栏 | U12 | ✅ |
| 无 regime 数据 → 不抛错跳过 tighten_stop | U13 | ✅ |
| must_act 排序 tighten>reduce>raise>cancel | U11 | ✅ |
| 响应 schema camelCase 严格匹配 API-CONTRACT line 1592-1631 | I5 | ✅ |
| DB 异常 → 500 + 标准 APIError 体 | I6 | ✅ |

---

## API 契约一致性

- 顶层 `{ data: { asOfDate, mustAct, monitor, noAction }, message }` ✅
- item 字段 `{ ticker, actionType, rationale, refs }` ✅
- camelCase（D074）✅
- 不嵌 `items` 包裹（与 GET /pending-orders 一致）✅

---

## 决策记录

contract §7 Q1-Q8 已写入 `DECISIONS.md` D075。本验收无新增决策点。

---

## 已知遗留 / 备注

1. **函数行数**：`_classify_position`（~72 行）和 `build_today_actions`（~70 行）略超 50 行内聚上限。Evaluator 评估为"逻辑内聚，强行拆分降低可读性"，本次保留；若未来 actionType 优先级再增可拆分。
2. **ruff 未配置**：项目 dev 依赖未含 ruff。本 sprint 由 Evaluator 做手动代码质量检查（无死代码 / 无魔法值 / 无重复实现 LastCloseLoader 与 compute_next_action）。后续可在 v2.x 单独引入 ruff。
3. **F207 父 feature 仍处 needs_review**：前端 F207-b（ActionListWidget UI）尚未开发，F207 整体 phase 维持 `needs_review`，仅子 sprint F207-a phase → done。

---

## 后续

- 触发 `/feature-dev F207-b`（前端 ActionListWidget）。
- 不发版（v1.9 待 F207-b 完成后整体打 tag）。

