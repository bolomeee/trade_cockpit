# 验收记录 — F217-c1

**项目**：MA150 Tracker → Workbench
**Feature**：F217-c1 — Cockpit Phase C 后端 capitulationEvidence 接口实装
**版本**：v2.2.0
**验收日期**：2026-05-18
**验收人**：用户确认
**Sprint Contract**：docs/开发/sprint-contracts/F217-c1-contract.md

---

## 验收结论

**通过** ✅

---

## Consistency Check 入口扫描

- 模式：report
- 🔴 严重违例：0
- 🟡 中等违例：0
- 🟢 轻微违例：2（预存/预期：C4 iteration_history 覆盖 + C5 F217-c2 design_needed 无合约文件）
- 结论：可进入验收

---

## 测试通过情况

| 测试 | 覆盖内容 | 结果 |
|------|---------|------|
| T1（4 cases） | CapitulationEvidence schema 构造 + camelCase 别名（volZscore / drop5dPct / reversalDay） | ✅ 4/4 |
| T2（3 cases） | compute_capitulation_evidence happy path，drop_5d_pct 1 位小数 + reversal_day=True | ✅ 3/3 |
| T3（5 cases） | helper 返回 None：bars<6 / closes[-6]=0 / 空列表 + 边界值 | ✅ 5/5 |
| T4（2 cases） | reversal_day=False 分支（close 在下 2/3） | ✅ 2/2 |
| T5（2 cases） | CAPITULATION + vol_zscore 充足 → evidence 非 null，response camelCase 正确 | ✅ 2/2 |
| T6（2 cases） | BREAKOUT / RECLAIM → capitulation_evidence=None | ✅ 2/2 |
| T7（1 case） | vol_zscore=None → capitulation_evidence=None（防御 NP-c1-3=A） | ✅ 1/1 |
| T8（1 case） | DailyBar 仅 3 行（bars 不足）→ capitulation_evidence=None | ✅ 1/1 |
| **T9 全量回归** | 1115 passed / 8 预存失败 / **0 新增失败** | ✅ |
| **合计** | | **20/20** |

---

## Evaluator 自检清单

- [x] 单元测试 T1-T4 全部通过（14/14）
- [x] 集成测试 T5-T8 全部通过（6/6）
- [x] test_capitulation_reversal.py 34 tests 全过（guard — _is_capitulation_reversal 未改动）
- [x] 全量回归 1115 passed 8 预存失败 0 新增失败
- [x] API-CONTRACT L1396-1424 字段名一致（capitulationEvidence / volZscore / drop5dPct / reversalDay）
- [x] DecisionData / CapitulationEvidence model_dump(by_alias=True) 正确（drop5dPct 显式 alias 修复 to_camel 大写 D 问题）
- [x] 无 DB 改动（0 alembic 文件，不需要迁移）
- [x] _is_capitulation_reversal 行为未变
- [x] purge_legacy_pullback 字节级别未变
- [x] ruff 0 新增 warning（基线 position.py F401 预存）
- [x] DECISIONS.md 无新决策追加（D095 已覆盖）
- [x] git status 清，4 文件全 commit（dcadcf6）

---

## 实装文件清单（4 文件）

| 文件 | 改动 |
|------|------|
| `backend/app/schemas/cockpit/decision.py` | 新增 `CapitulationEvidence` 模型（3 字段，drop5dPct 显式 alias）+ `DecisionData.capitulation_evidence` 字段 |
| `backend/app/services/cockpit/setup_service.py` | 新增 module-level `compute_capitulation_evidence(closes, highs, lows) -> dict \| None` |
| `backend/app/services/cockpit/decision_service.py` | CAPITULATION 分支 evidence 填充（inline DailyBar select + helper 调用 + 3 防御边界） |
| `backend/tests/test_decision_f217c1.py` | 新建（T1-T8，20 tests） |

---

## 关键实现决策（沿用 Sprint Contract NP）

- **NP-c1-1=A**：evidence 数据来源 decision_service 实时查 DailyBar，volume_zscore 直接读 snapshot 列
- **NP-c1-3=A**：三个防御边界（vol_z None / helper None / bars 不足）均返回 evidence=None，不展示 N/A chip
- **NP-c1-4=A**：inline select 风格与现有 _earnings_risk_and_date 一致
- **NP-c-5=A**：volume_zscore 直接读 setup_snapshot.volume_zscore（已存在列，不重算）
- **drop5dPct 别名修复**：to_camel 将 drop_5d_pct 产 drop5DPct（大写 D），显式 Field(alias="drop5dPct") 与 API-CONTRACT 对齐

---

## 下一步

F217-c2 Sprint Contract 协商（前端 chips + badge + token + design-spec）：
- DecisionPanelWidget.tsx：setupType=CAPITULATION 时展示 3 chips
- SetupTypeBadge.tsx：CAPITULATION 紫色 badge
- tokens.css：--color-setup-pullback → --color-setup-capitulation
- design-spec.md：setup color 表 + chips 视觉规格
- 前端类型扩展：cockpitDecisionApi CapitulationEvidence / setupMonitorApi SetupType
- 5 个测试 fixture PULLBACK → CAPITULATION（union 收紧连锁修复）
- 预估 9-11 文件，协商时评估是否需拆 c2a/c2b
