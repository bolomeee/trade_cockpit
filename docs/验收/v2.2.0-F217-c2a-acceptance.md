# 验收记录 — F217-c2a

**Feature**：F217 Cockpit Phase C — Capitulation Reversal 严格重写  
**Sub-sprint**：F217-c2a（前端 CAPITULATION 渲染 additive）  
**版本**：v2.2.0  
**验收日期**：2026-05-18  
**验收结论**：✅ 通过

---

## 实现范围

- `cockpitDecisionApi.ts`：新增 `CapitulationEvidence` 类型 + `capitulationEvidence?: CapitulationEvidence | null`
- `setupMonitorApi.ts`：`SetupType` union 追加 `'CAPITULATION'`（PULLBACK 暂保留向后兼容）
- `SetupTypeBadge.tsx`：CAPITULATION 追加至 union + TYPE_COLORS（`var(--color-setup-capitulation)`）+ TYPE_LABELS（`CAP_REV`）
- `DecisionPanelWidget.tsx`：setupType=CAPITULATION + capitulationEvidence 非 null 时渲染 3 个证据 chip（Vol z-score / Drop 5d / Reversal day）
- `tokens.css`：新增 `--color-setup-capitulation: #8b5cf6`
- 新建 `DecisionPanelWidget.capitulation.test.tsx`：T1-T6 专项测试全绿

## 验收检查结果

| 检查 | 结果 |
|------|------|
| TSC 0 错误 | ✅ |
| T1-T6 capitulation 专项测试 | ✅ 7/7 |
| 全量测试套件 | ✅ 22/28 suites（= 基线，0 新增失败）|
| SetupTypeBadge CAPITULATION token 注册 | ✅ `--color-setup-capitulation: #8b5cf6` |
| DecisionPanelWidget 3 chips 逻辑 | ✅ 单元测试覆盖，实时数据极稀疏（预期）|

## 遗留注意事项

- PULLBACK 在 c2a 阶段故意保留（向后兼容），c2c 完成退场
- CAPITULATION 触发极稀疏（7 条 AND 门），实时 UI 验证依赖单元测试
