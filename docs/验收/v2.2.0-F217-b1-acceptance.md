# 验收记录 — F217-b1 Cockpit Phase C DB 层

**版本**：v2.2.0（进行中）
**Feature**：F217-b1 — setup_snapshots.legacy 列 + PULLBACK 软删（Phase C DB 层）
**日期**：2026-05-15
**验收人**：用户确认
**验收结论**：✅ 通过

---

## 验收范围

Sprint Contract：[docs/开发/sprint-contracts/F217-b1-contract.md](../开发/sprint-contracts/F217-b1-contract.md)

**实现文件（5 个）**：
1. `backend/alembic/versions/021_f217b1_setup_snapshots_legacy.py` — 新建
2. `backend/app/models/setup_snapshot.py` — 加 `legacy` 字段
3. `backend/app/repositories/setup_snapshot_repository.py` — 加 `purge_legacy_pullback()` + legacy 过滤
4. `backend/app/services/cockpit/decision_service.py` — select 加 legacy 过滤
5. `backend/tests/test_setup_snapshot_purge.py` — 新建（12 条 integration tests）

---

## 测试结果

| 测试类型 | 结果 |
|---------|------|
| F217-b1 集成测试（12 条） | ✅ 12/12 passed |
| 全量后端回归 | ✅ 1095 passed，0 新增失败（8 预存失败非本 sprint 引入）|
| ruff lint（5 文件） | ✅ 0 warning |

### T1-T9 测试覆盖

| # | 测试 | 结果 |
|---|------|------|
| T1 | alembic 021 upgrade/downgrade/upgrade 闭环 | ✅ |
| T2 | upgrade 后 PULLBACK 行 legacy=1，其它行 legacy=0 | ✅ |
| T3 | `purge_legacy_pullback()` 幂等（第二次 rowcount=0） | ✅ |
| T4 | purge 不误标 BREAKOUT/NONE/CAPITULATION | ✅ |
| T5 | `get_latest_all_active` 过滤 legacy=true 行（不 fallback） | ✅ |
| T5b | get_latest 返回更旧的 legacy=false 行而非新的 legacy=true 行 | ✅ |
| T6 | `get_latest_for_tickers` 与 `get_latest_all_active` 行为一致 | ✅ |
| T7a | decision_service 对 legacy=true 行抛 LookupError（→ 404） | ✅ |
| T7b | decision_service 有 legacy=false 旧行时正确返回旧行 | ✅ |
| T8a | upsert_batch 新插入行 legacy=False | ✅ |
| T8b | upsert_batch on-conflict 不覆盖 legacy 值 | ✅ |
| T9 | `delete_before` 不区分 legacy 状态 | ✅ |

---

## 静态验证

| 项目 | 结果 |
|------|------|
| `select(SetupSnapshot)` 全路径扫描（共 2 处） | ✅ 均加 `.where(legacy==False)` |
| PULLBACK 字符串仅在迁移/purge/测试出现 | ✅ |
| Pydantic Literal 未改动（F217-b2 解耦） | ✅ |
| alembic `server_default` 迁移后立刻撤销 | ✅ NP-b1-1 |
| downgrade 只 drop_column，不自动撤销 UPDATE | ✅ NP-b1-2 |

---

## 后续

- **F217-b2**（下一 sprint）：Pydantic Literal 扩展 — 6 schemas 加 CAPITULATION 字面量，暂保留 PULLBACK 向后兼容
- F217-b1 上线后 SetupMonitor 立刻能显示 CAPITULATION 行（如有触发），但紫色 badge 要等 F217-c
- 建议观察下一个 daily cron 后确认历史 PULLBACK 行已从 setup_monitor 消失

---

## Git 提交

`feat(F217-b1): setup_snapshots.legacy column + PULLBACK soft-delete (Phase C DB layer)` — `9ff5ac2`
