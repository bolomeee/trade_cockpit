━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-21] Sprint：F106-a 多信号扫描核心（后端）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容：
  - scanner_params.py 新增：4 种 signal_type 枚举 + A1/A2/B2 阈值 + 共享参数（FETCH_WINDOW=90 / SCAN_WORKER_COUNT=6）
  - market_scanner_service.py 重构：_BarCtx 不可变 dataclass + 4 个独立 detector（legacy / a1_stage_breakout / a2_slope_flip / b2_ma_pullback）+ hits_by_type 聚合
  - market_breakout_scan 模型：加 signal_type / volume / volume_ratio_20；唯一键改 (scan_date, ticker, signal_type)
  - repository：get_latest_snapshot 支持 signal_types 过滤
  - alembic 003 迁移：batch_alter_table 改唯一键 + 三字段，默认回填 legacy_crossover
  - 新增测试：test_market_scanner_detectors.py（10 用例覆盖 legacy/A1/A2/B2 正向+边界）
  - 新增测试：test_schema.py::test_migration_003_... （upgrade→downgrade 往返验证）
  - 修复：测试文件 8 处因 BreakoutScanRow/API 字段扩展引入的 schema 不一致
  - F106-a scope 测试：48/48 全绿
  - 全量回归：243/245（2 失败均为 F108 scope：test_stock_detail.py 的 pullbacks 404 期望，已在 F108-contract 中计入）
  - phase：F106-a → needs_review

⚠️ 待你决策：
  - 验收 F106-a（Evaluator 自检已通过；2 个 F108-scope 回归失败由 F108 sprint 修复）
  - F106-a 验收后，按 F106-b contract 进入（router ?type= + schemas + fmp_client SMA 窗口）

🔧 系统变更：
  - 新文件：backend/app/services/scanner_params.py
  - 新文件：backend/alembic/versions/003_f106_signal_type_and_volume.py
  - 新文件：backend/tests/test_market_scanner_detectors.py
  - 表结构：market_breakout_scans 加 signal_type(NN)/volume/volume_ratio_20，唯一键 uq_breakout_scan_date_ticker → uq_breakout_scan_date_ticker_signal

📊 功能状态：
  F106  Multi-Signal Scanner：🔄 in_progress（a done / b,c contract_agreed）
    F106-a 扫描核心：🔍 needs_review
    F106-b 后端 API：⬜ contract_agreed
    F106-c 前端 widget：⬜ contract_agreed
  F107  Chart volume + MA5/20：⬜ ready_to_dev（design-spec 需更新）
  F108  Fundamentals 放开：⬜ contract_agreed（D047）
  F109  Widget 间距 + TopNav：⬜ contract_agreed（a/b 反向补）

[下一个 Session]
  验收 F106-a 后继续 F106-b（Generator 模式）：改 router /api/market/breakouts ?type= / 扩 schemas / fmp_client SMA 窗口 35→90

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-22] 验收：F106-a 合格 → done
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- F106-a phase：needs_review → done
- 下一步：按 F106-b contract 进入 Generator 模式

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-22] Sprint：F106-b 多信号扫描 API 层（后端）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容（反向补契约 Evaluator 模式，业务代码未动）：
  - 核对：routers/market.py 的 ?type= 解析 + 400 校验、schemas/market.py 的 4 个新字段、fmp_client 的 SMA 35→90 + is_fund/is_etf Optional 改造均匹配 contract
  - 新增测试（test_market_api.py 6 用例）：
    · 默认过滤掉 legacy_crossover
    · ?type=legacy_crossover 只返 legacy
    · ?type=a1_stage_breakout,b2_ma_pullback 组合过滤
    · ?type=foo → 400 + detail
    · ?type= 空串 → 降级默认
    · items[] 含 signalType/slopeValue/volume/volumeRatio20
  - 新增测试（test_fmp_client.py 3 用例）：
    · is_etf=None 不下传 isEtf
    · is_fund=False/True 正确序列化
    · get_screener_universe 对 3 个交易所都下发 isFund=false 且不下发 isEtf
  - 修文档：API-CONTRACT.md GET /api/market/breakouts 错误码 422 → 400（与实现一致，用户选项 C）
  - 全量回归 252/254（2 失败为 F108 scope）
  - phase：F106-b → needs_review

⚠️ 待你决策：
  - 验收 F106-b 后进入 F106-c（前端 Tabs + MarketBreakoutWidget 重构）

🔧 系统变更：
  - 业务代码零改动（反向补 Evaluator）
  - 新增测试 9 用例
  - API-CONTRACT.md 错误码修正

📊 功能状态：
  F106  Multi-Signal Scanner：🔄 in_progress（a done / b needs_review / c contract_agreed）
    F106-a 扫描核心：✅ done
    F106-b 后端 API：🔍 needs_review
    F106-c 前端 widget：⬜ contract_agreed

[下一个 Session]
  验收 F106-b 后进入 F106-c（Evaluator 模式核对前端 Tabs 实现）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-22] 验收：F106-b 合格 → done
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- F106-b phase：needs_review → done
- 下一步：按 F106-c contract 进入 Evaluator 模式（前端 Tabs + MarketBreakoutWidget 反向补契约）
- 环境说明：5173 Vite dev server 不受 docker 管理；用户本地已重启 pnpm dev，工作树内 F106-c/F107/F108/F109 前端改动为用户既有未提交状态

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-22] Sprint：F106-c 多信号 Widget（前端）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容（反向补契约 Evaluator 模式）：
  - 核对 4 个目标文件与 Contract 逐条对齐：
    · tabs.tsx（shadcn 官方 radix-ui barrel + cva default/line variants + data-slot/group 模式，Context7 核验）
    · types/market.ts（SignalType 联合类型 + BreakoutItem 4 新字段）
    · lib/api/market.ts（getBreakouts(types?) 可选过滤，空数组/undefined → 不带 qs 走后端默认）
    · MarketBreakoutWidget.tsx（Tabs 两栏 / BreakoutPane 抽子组件 / TAB_SIGNALS / typesKey 排序拼接 / key=${ticker}-${signalType} / 动态 Signal 列 / volumeRatio20 空值 — 非空 N.NN× / 空态分 Tab 文案 / + 按钮 h-5 w-5 + stopPropagation / 容器 marginTop -5 marginLeft -5 gap-1）
  - Lint 修复：tabs.tsx 移除 `tabsListVariants` 导出（react-refresh/only-export-components，用户选项 A，仅内部引用、零外部消费者）
  - 降级 Contract（D048）：项目无 vitest/@testing-library 基建，Contract #3–#8 的 6 条 RTL 单测改为代码审查 + 手工回归；#9–#11 保留为硬门禁（用户选项 B）
  - 硬门禁全部通过：
    · tsc -b 通过
    · vite build 通过（483ms，bundle 正常）
    · docker compose E2E：frontend 8080 → 200；stage Tab `?type=a1_stage_breakout,a2_slope_flip` → 19 命中；pullback Tab `?type=b2_ma_pullback` → 45 命中（2026-04-21 真实扫描数据）
  - 全量 lint：3 problems（2 errors + 1 warning），均为 pre-F106-c 预先存在（button.tsx buttonVariants 导出 / JournalEntryForm / MarketSignalSearch）
  - phase：F106-c → needs_review

⚠️ 待你决策：
  - 验收 F106-c 后，F106 主 feature（a/b/c 三子任务均 done）可整体流转到 done

🔧 系统变更：
  - frontend 业务代码仅 1 处改动：tabs.tsx export 清单（移除 tabsListVariants）
  - F106-c-contract.md 测试门禁降级 + Evaluator 自检清单全部打勾
  - DECISIONS.md 新增 D048：前端单测基建延迟到 v1.4（F107/F108/F109 按同样路径降级）

📊 功能状态：
  F106  Multi-Signal Scanner：🔄 in_progress（a/b done, c needs_review）
    F106-a 扫描核心：✅ done
    F106-b 后端 API：✅ done
    F106-c 前端 widget：🔍 needs_review
  v1.4 待规划：前端测试基建（vitest + @testing-library/react + jsdom）

[下一个 Session]
  验收 F106-c 后，F106 整体 done；下一个反向补契约：F107（股价图成交量 + MA5/20 短均线）
