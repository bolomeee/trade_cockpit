━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-05-13] ✅ 验收通过：F215-b Volume Accumulation 三件套 + BREAKOUT 吸筹门槛
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ F215 全部完成（F215-a + F215-b）

完成内容：
  - setup_snapshots 新增 3 列：volume_zscore / obv_trend / up_down_volume_ratio
  - 三项纯函数：_compute_volume_zscore / _compute_obv_trend / _compute_up_down_volume_ratio
  - BREAKOUT 吸筹门槛（D088）：z≥1.5 AND U/D≥1.2，未达标降级为 NONE
  - API 新增 3 camelCase 字段，SetupMonitorWidget 新增 Vol Z 列
  - alembic 018 migration 已在本地执行

验收发现：
  - 需手动指定 `alembic upgrade 018_f215b_setup_volume_accumulation`（head 有游离分支）
  - 需手动触发一次 compute_and_store_all 填充存量快照

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-05-13] Sprint Contract 协商：F215-b Volume Accumulation 三件套
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容：
  - F215-b Sprint Contract 协商完成，用户按推荐方案确认全部 7 个协商点
  - 确认方案 A（7 文件，cockpit_params 集中配置）
  - z-score 窗口 N=50，OBV 比较法 FLAT 阈值 2%，U-D ratio 分母为零 return None
  - obv_trend VARCHAR(4)，历史快照不回填，Vol Z 列仅展示
  - features.json F215-b phase → contract_agreed
  - Sprint Contract 路径：docs/开发/sprint-contracts/F215-b-contract.md

📊 功能状态：
  F215-a (Risk cap + EMA 10/21)：✅ done
  F215-b (Volume Accumulation)：🟡 contract_agreed → 待 Generator 模式开发

[下一个 Session]
  继续开发 F215-b，Sprint Contract 已确认。
  读取 SESSION-HANDOFF.md + docs/开发/sprint-contracts/F215-b-contract.md，
  进入 Generator 模式，从开发步骤 1 开始。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-05-11] Bug fix：ChartWidget 按钮 + Regime 刷新
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容：
  - F214 CirclePlus 按钮位置调整：从右下角移至 ticker 标签旁边（顶部左侧内联）
  - Bug fix：按钮无法点击 — 根因是 lightweight-charts canvas z-index:2 与 overlay z-index:2
    相同，canvas 因 DOM 顺序靠后覆盖按钮；将 overlay zIndex 改为 3 修复
  - Bug fix：Market Regime 数据停留在 2026-04-25（16 天未更新）
    根因：regime refresh 只有 22:15 UTC cron，无手动触发入口且主刷新未包含
    修复：
      1. 新增 POST /api/admin/refresh-regime endpoint（admin.py）
      2. RefreshJobManager._run() 内嵌 regime ETF refresh + compute（refresh_job.py）
      3. useRefreshStatus.ts 完成时 invalidate ['cockpit', 'regime'] 触发前端更新
  - API-CONTRACT.md：补全 Admin 章节（5 个 endpoint 均已文档化）

📊 文件变更：
  - frontend/src/workbench/widgets/ChartWidget.tsx（zIndex 2→3，按钮移位）
  - frontend/src/hooks/useRefreshStatus.ts（invalidate regime query）
  - backend/app/routers/admin.py（新增 refresh-regime endpoint）
  - backend/app/services/refresh_job.py（_run() 内嵌 regime refresh）
  - docs/系统设计/API-CONTRACT.md（Admin 章节）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-05-08] Sprint：F214 ChartWidget Add to Watchlist 按钮
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容（Contract 协商阶段）：
  - 读取 ChartWidget.tsx / WatchlistWidget.tsx / AddStockCard.tsx / watchlist.ts API 定义
  - 确认 ChartWidget 是 News 和 Workbench 共用同一组件（WidgetRegistry.ts:26 / :76）
  - 确认 addStock API 现成，无需后端改动
  - 确认 useQuery(['signals']) 已在 ChartWidget 中使用，可零成本判定 isInWatchlist
  - 用户确认 1A（两页都显示）/ 2b（disabled 反馈已在 watchlist）/ 3 简单（无成功 toast）
  - Sprint Contract 起草并经用户确认
  - features.json 加入 F214 entry，phase = contract_agreed
  - docs/开发/sprint-contracts/F214-contract.md 已写

📊 文件清单（预计修改 2 个，远低于 6 文件上限）：
  - frontend/src/workbench/widgets/ChartWidget.tsx（修改）
  - frontend/src/workbench/widgets/__tests__/ChartWidget.test.tsx（新建）

📊 功能状态：
  F213 News Article Auto-Translate：✅ done
  F214 ChartWidget Add to Watchlist：📝 contract_agreed（待 Generator）

[下一个 Session]
  用 Sonnet 开新 session，粘贴：
  > 继续开发 F214，Sprint Contract 已确认。
  > 读取 SESSION-HANDOFF.md + docs/开发/sprint-contracts/F214-contract.md，
  > 进入 Generator 模式，从开发顺序步骤 1 开始。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-05-12] Sprint：F215-a Cockpit Phase A — Risk cap + EMA 10/21（Generator + Evaluator 完成）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容（Generator + Evaluator 阶段）：
  - API-CONTRACT.md：§GET /api/cockpit/chart/{ticker} 追加 emas 字段 + 服务端行为说明
  - cockpit_params.py：RISK_ON 1.5% → 1.25%（对齐 SRS 第十节）；CHART 新增 DEFAULT_EMAS=[10,21]
  - chart_service.py：新增 _compute_ema_series(bars, period)（α=2/(period+1)，seed=SMA(period)）；get_chart() 返回 emas
  - schemas/cockpit/chart.py：CockpitChartData 新增 emas: dict[str, list[ChartSeriesPoint]]
  - routers/cockpit/chart.py：emas_out 串联到 CockpitChartData 构造
  - cockpitChartApi.ts：CockpitChartData type 增加 emas 字段
  - CockpitChartWidget.tsx：EMA10/EMA21 虚线（LineStyle.Dashed）渲染 + header 图例
  - MarketRegimeWidget.tsx：toFixed(1) → toFixed(2)（RISK_ON 1.25% 精确显示）
  - tests/test_f215a.py（后端）：12/12 通过（EMA 单元/集成/路由，RISK_ON 1.25 断言）
  - CockpitChartWidget.test.tsx（前端）：S9 EMA 渲染测试 + mockChartData 加 emas
  - MarketRegimeWidget.test.tsx（前端）：toFixed(2) 精度测试 + RISK_ON 1.25% 测试
  - DECISIONS.md：追加 D085（RISK_ON 上限调整）+ D086（EMA 序列共存决策）
  - Evaluator 通过：全量后端 6 failed（全预存在），全量前端 7 failed（全预存在），F215-a 零新增失败
  - sub_sprints.F215-a → done；F215.phase → in_progress（F215-b 待完成）

⚠️ 已知预存在测试失败（非 F215-a 引入）：
  后端：test_s14_cockpit_params（INDEX_ETFS 长度）/ test_s4_indices / test_R6_news_summarizer / test_fmp_screener
  前端：AiNewsSummaryBar C1-C8 / SetupMonitorWidget §S / TopNav S12-S13 / DecisionPanelWidget S4

🔧 文档变更：
  - API-CONTRACT.md（emas 字段说明）
  - DECISIONS.md（D085 + D086）

📊 功能状态：
  F214 ChartWidget Add to Watchlist：🔍 needs_review
  F215 Cockpit Phase A：🔄 in_progress
    ├─ F215-a Risk+EMA: ✅ done（待验收）
    └─ F215-b Volume Accumulation: ⬜ design_needed

[下一个 Session]
  F215-b Volume Accumulation Sprint Contract 协商，或 F215-a 验收。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-05-12] Sprint：F215-a Cockpit Phase A — Risk cap + EMA 10/21（Contract 协商阶段记录）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容（Contract 协商阶段）：
  - 完整改善计划已批准：/Users/wonderer/.claude/plans/featrue-dev-replicated-goblet.md
    （对照 SRS 框架 4 个阶段：A=Quick Wins / B=Weekly Stage / C=Capitulation 重写 / D=Repricing Trigger 5 类）
  - Phase A 按 6-file 原则拆为 F215-a (Risk+EMA, 6 文件) + F215-b (Volume Accumulation, 6 文件，含 alembic 018)
  - features.json 新增 F215 entry，sub_sprints={F215-a: contract_agreed, F215-b: design_needed}
  - Sprint Contract F215-a 已起草并经用户确认（按推荐方案）
  - 三个协商点已确认：
      1) MarketRegimeWidget.tsx 改 toFixed(2) 让 1.25 精确显示（文件清单第 6 个）
      2) EMA 复用 SMA 颜色 token + LineStyle.Dashed 区分（零 tokens.css 变更）
      3) EMA 周期固定 [10,21]，不开放 ?emas= 查询参数

📊 文件清单（预计修改 6 个，命中 6-file 上限）：
  - backend/app/services/cockpit/cockpit_params.py（修改：RISK_ON 1.5→1.25；CHART 新增 DEFAULT_EMAS=[10,21]）
  - backend/app/services/cockpit/chart_service.py（修改：新增 _compute_ema_series + get_chart 返回 emas）
  - backend/app/schemas/cockpit/chart.py（修改：CockpitChartData 加 emas 字段）
  - frontend/src/cockpit/lib/api/cockpitChartApi.ts（修改：type 加 emas）
  - frontend/src/cockpit/widgets/CockpitChartWidget.tsx（修改：渲染 EMA10/EMA21 虚线）
  - frontend/src/cockpit/widgets/MarketRegimeWidget.tsx（修改：toFixed(1) → toFixed(2)）

📊 功能状态：
  F214 ChartWidget Add to Watchlist：🔍 needs_review
  F215 Cockpit Phase A：📝 contract_agreed
    ├─ F215-a Risk+EMA: 📝 contract_agreed（待 Generator）
    └─ F215-b Volume Accumulation: ⬜ design_needed

🔧 文档同步计划（Generator 阶段执行）：
  - 开发前：API-CONTRACT.md §GET /api/cockpit/chart/{ticker} 追加 emas 字段
  - 开发后：DECISIONS.md 追加 D076（RISK_ON 1.25%）+ D077（EMA 序列加入 chart）

[下一个 Session]
  用 Sonnet 开新 session，粘贴：
  > 继续开发 F215-a，Sprint Contract 已确认。
  > 读取 SESSION-HANDOFF.md + docs/开发/sprint-contracts/F215-a-contract.md，
  > 进入 Generator 模式，从开发顺序步骤 1（API-CONTRACT.md 文档同步）开始。

════════════════════════════════════════
2026-05-12 | F215-a 验收通过
════════════════════════════════════════

✅ F215-a Risk cap (1.5%→1.25%) + EMA 10/21 验收通过

📋 验收结果：
  - EMA10/EMA21 虚线正确渲染在 CockpitChartWidget 图例和 chart 上
  - API /api/cockpit/chart/{ticker} 返回 emas 字段（已验证 NVDA: ema10=241点，ema21=230点）
  - MarketRegimeWidget toFixed(2) 精度正确
  - 无 console.error；无功能回归
  - 后端 test_f215a.py 12/12 通过；前端 CockpitChartWidget/MarketRegimeWidget F215-a 专项测试全通过
  - DB 快照仍显示 1.50%（预期，下次 compute_and_store 自动修正，D085 有记录）

📊 功能状态更新：
  F215 Cockpit Phase A：🔄 in_progress（F215-b 待设计）
    ├─ F215-a Risk+EMA: ✅ done（2026-05-12）
    └─ F215-b Volume Accumulation: ⬜ design_needed

📝 生成文件：
  - docs/验收/v1.9-F215-a-acceptance.md

[下一步]
  F215-b Volume Accumulation 三件套（z-score/OBV/up-down volume）
  在新 session 中进入 system-design/feature-dev 规划。
