━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-27] Sprint：F205-a Universe 字段扩展（Contract 协商）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容（本 session 仅协商，未编码）：
  - 读完 F205 全部前置文档（API-CONTRACT §Pool / DATA-MODEL §MarketScanUniverse+§MarketBreakoutScan+§SetupSnapshot / design-spec Widget3 / data-mapping §Cockpit-3 / component-plan §Cockpit-2~5）
  - 对比 FMP client 现有能力 + 现有数据底座，识别 F205 数据源 4 个 gap：sector / price / ADV / revenue_growth
  - 与用户确认 4 个技术决策：
      D1（trend 层语义）= A：复用 market_breakout_scans 命中
      D2（sector 字段持久化位置）= A：market_scan_universe 加 4 列
      D3（distanceTo50maPct 算法）= A：rs/fundamental 子集 EOD 调用副产品
      D4（拆分）= 同意 a/b/c/d 四个 sub_sprint
  - 起草 F205-a Sprint Contract（universe 字段扩展），新增字段级降级聚合日志要求（用户提醒补强）
  - features.json：F205 加 sub_sprints 结构，F205-a phase=contract_agreed，active_sub_sprint=F205-a
  - _pipeline_status：active_sprint 由 F207-b → F205-a，current_iteration v2.0 → v1.9（v1.9.0 优先于 v2.0）
  - last_updated：2026-04-27-F205-a-contract_agreed

⚠️ 待你决策：
  - 无（Sprint Contract 已确认）

🔧 系统变更：
  - 新建 docs/开发/sprint-contracts/F205-a-contract.md
  - 修改 docs/需求/features.json（F205 sub_sprints + _pipeline_status）

📊 功能状态：
  F207 整体：phase=done（两个 sub_sprint 等 acceptance）
  F205 整体：phase=in_progress，active_sub_sprint=F205-a (contract_agreed)
  F205-a：contract_agreed
  F205-b/c/d：planned

[下一个 Session]
  开新 Sonnet session，粘贴：
  > 继续开发 F205-a，Sprint Contract 已确认。
  > 读取 SESSION-HANDOFF.md + docs/开发/sprint-contracts/F205-a-contract.md，
  > 进入 Generator 模式，从开发步骤 1 开始。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-27] F205-a Generator — 开发完成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Sprint Contract：F205-a（universe 字段扩展）
执行模式：Generator（新 session，续上次 contract_agreed 中断）

完成内容：
1. backend/alembic/versions/015_f205a_universe_extra_fields.py（新建）
   - upgrade：market_scan_universe 加 sector/industry/last_price/last_volume（4 nullable 列）
   - downgrade：drop 4 列
2. backend/app/models/market_scan_universe.py（修改）
   - 新增 sector(String 64)/industry(String 128)/last_price(Float)/last_volume(BigInteger)，全部 nullable
3. backend/app/repositories/market_scan_universe_repository.py（修改）
   - UniverseUpsertRow 加 4 个 Optional 字段（默认 None）
   - upsert_many：INSERT + ON CONFLICT DO UPDATE 两侧写入 4 列
4. backend/app/services/universe_refresh_service.py（修改）
   - _parse_screener_row：解析 sector/industry/price/volume，缺字段/类型错降级为 None 不跳过
   - _PARSE_EXCEPTION_COUNTER：模块级 mutable list，记录未预期解析异常
   - refresh()：累加 4 个字段级 counter，写入 OK SystemLog 末尾；parse_exception > 0 时额外写 WARN log
5. backend/tests/test_market_scan_repositories.py（修改）
   - 更新 schema 列集合断言；新增 ORM 属性/parse/upsert 单元测试（+10 个）
6. backend/tests/test_universe_refresh_service.py（修改）
   - 新增集成测试：新字段写入、降级 counter log、no-WARN gate、parse_exception WARN（+4 个）
7. backend/tests/test_schema.py（修改）
   - market_scan_universe 列集合同步（发现并修复 Evaluator 阶段）
8. docs/系统设计/DATA-MODEL.md — MarketScanUniverse 字段表加 4 行 + 业务规则段更新
9. docs/系统设计/DECISIONS.md — 追加 D078

测试结果：
  - 目标测试：28/28 ✅
  - 全量回归：758 passed, 0 failed ✅
  - Alembic 迁移：通过（test_schema.py 使用完整 Alembic 链验证 upgrade 015）

F205-a phase → needs_review

[下一步]
  F205-a 待 acceptance 验收。
  下一个开发 sprint：F205-b（FMP 增量 client 方法 + RS percentile / 50ma / fundamental helper）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-27] F205-a 验收通过
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

F205-a phase → done
验收记录：docs/验收/v1.9-F205-a-acceptance.md

active_sprint → F205-b（planned）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-27] Sprint Contract 协商：F205-b
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Sprint Contract 已确认：
  - 文档：docs/开发/sprint-contracts/F205-b-contract.md
  - 范围：FMP 客户端增量（get_financial_growth）+ pool_helpers.py 5 个纯函数
  - 关键定位：F205-b = building blocks（pure helpers），F205-c 才是 PoolService + endpoint
  - setup_service.py 不动（保留双 RS 实现，dedup 列入 D079 技术债）
  - 文件计数：4/6（远在硬上限内）

预计修改文件：
  1. backend/app/external/fmp_client.py（修改）
  2. backend/app/services/cockpit/pool_helpers.py（新建）
  3. backend/tests/test_pool_helpers_f205b.py（新建）
  4. backend/tests/test_fmp_client.py（修改）

附加：DECISIONS.md 加 D079（FMP financial-growth；RS 公式；fail-open；双实现技术债）

⚠️ 待确认（Generator 阶段执行时）：
  - FMP /stable/financial-growth 字段名最新版（用 context7 查证）
  - get_financial_growth 错误处理风格与现有 ratios-ttm 对齐（Generator 阶段 grep 现有模式）

F205 sub_sprints 状态：
  F205-a: done
  F205-b: contract_agreed ← 当前
  F205-c: planned
  F205-d: planned

[下一个 Session]
  Sonnet 新 session，粘贴：
  > 继续开发 F205-b，Sprint Contract 已确认。
  > 读取 SESSION-HANDOFF.md + docs/开发/sprint-contracts/F205-b-contract.md，
  > 进入 Generator 模式，从开发步骤 1 开始。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-27] F205-b Generator 完成 → needs_review
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Sprint：F205-b FMP 增量 + Pool 计算 helpers
状态：needs_review（Evaluator 全部通过）

完成内容：
1. backend/app/external/fmp_client.py
   - 新增常量 FMP_EP_FINANCIAL_GROWTH = "/financial-growth"
   - 新增方法 get_financial_growth(symbol) -> dict | None
     * period=annual&limit=1，复用 token bucket + 6-concurrency semaphore
     * HTTPStatusError / RequestError → None（fail-open，D079）
     * 429 retry 由 _request 内部处理

2. backend/app/services/cockpit/pool_helpers.py（新建）
   - 纯函数模块，无 IO / DB / logger / httpx 依赖
   - 5 个函数：
     * compute_return_ratio_250d：250 日 stock/SPY return ratio；序列不足/spy≈0 → None
     * compute_rs_percentile_map：mid-rank percentile（与 setup_service 公式不同，见 D079）
     * compute_distance_to_50ma_pct：(close-ma50)/ma50×100；ma50 None/0 → None
     * extract_revenue_growth_yoy_pct：读 FMP revenueGrowth 字段 ×100
     * passes_fundamental_sanity：None growth → True（fail-open，D079）

3. backend/tests/test_pool_helpers_f205b.py（新建）
   - 31 个单元测试，覆盖 contract §3 第 5-17 条
   - 含 AST 静态验证（test #17）：确认无 app.*/logging/SQLAlchemy/httpx import

4. backend/tests/test_fmp_client.py（修改）
   - 新增 5 个 test_get_financial_growth_* 用例

5. docs/系统设计/DECISIONS.md（追加 D079）

测试结果：
  test_fmp_client.py: 54/54 ✅
  test_pool_helpers_f205b.py: 31/31 ✅
  全量回归: 794 passed（758 → 758+36）✅
  setup/scanner 回归: 52/52 ✅
  setup_service.py diff: 空（未修改）✅

重要决策：
  - RS percentile 使用 mid-rank（与 setup_service strictly-below 不同）
    * 原因：Contract test #8/9 明确指定 mid-rank 期望值 (16.67, 50.0, 83.33)
    * 已在 D079 文档化为技术债，dedup 推到后续 sprint
  - get_financial_growth 错误处理：catch HTTPStatusError/RequestError → None
    * 与 get_ratios_ttm 不同（后者传播错误），符合 fail-open 策略

git commits：
  d1d476d wip(F205-b): fmp_client get_financial_growth + pool_helpers pure module
  4528c3c wip(F205-b): pool_helpers tests + fmp_client get_financial_growth tests
  4d982d2 docs(F205-b): D079 financial-growth 来源、RS mid-rank、fail-open、技术债
  [最终 feat commit 见下次 git log]

F205 sub_sprints 状态：
  F205-a: done
  F205-b: needs_review ← 当前
  F205-c: planned（PoolService + GET /api/cockpit/pool）
  F205-d: planned（前端 PoolBuilderWidget）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-27] F205-b 验收通过 → done
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

验收结论：✅ 全部通过
验收记录：docs/验收/v1.9-F205-b-acceptance.md

F205 sub_sprints 状态：
  F205-a: done
  F205-b: done ← 本次
  F205-c: planned（下一目标）
  F205-d: planned

下一步：协商 F205-c Sprint Contract
  F205-c 内容：PoolService（编排 + 缓存）+ GET /api/cockpit/pool router

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-27] F205-c Sprint Contract 协商完成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Contract 文件：docs/开发/sprint-contracts/F205-c-contract.md（已确认）

实现范围：
  - PoolService（5 层漏斗：tradable → trend → rs → fundamental → action）
  - GET /api/cockpit/pool router + Pydantic schemas
  - 编排 F205-b helpers + FmpClient.get_financial_growth + FmpClient.get_daily_bars
  - Service 单元测试 + Router 集成测试

预计修改文件（6/6）：
  1. backend/app/services/cockpit/pool_service.py（新建）
  2. backend/app/routers/cockpit/pool.py（新建）
  3. backend/app/routers/cockpit/__init__.py（修改 — 注册 pool_router）
  4. backend/app/schemas/cockpit/pool.py（新建）
  5. backend/tests/test_pool_service.py（新建）
  6. backend/tests/test_cockpit_pool_router.py（新建）

5 项关键决策（用户已全部确认）：
  D080-1 ADV 实现 = 单日代理 last_price * last_volume（技术债，F205-x 重构为 20d 真值）
  D080-2 trendScoreMin 参数接受但忽略；trend 层 = "在最新 breakout_scans 出现"二元代理
  D080-3 POOL_TREND_CAP=200，按 market_cap 降序截断
  D080-4 FMP 调用走 ThreadPoolExecutor(max_workers=6)，与全局 _FmpRateLimiter
         singleton（300 rpm token bucket + 6-槽 semaphore）协作；接受"pool 请求
         期间挤占其他 FMP 消费者"取舍
  D080-5 仅请求级 in-memory 缓存，不落 DB（DailyPayloadCache 模式归 F205-x）

非 watchlist ticker 字段映射：
  setupType / trendScore / distanceToPivotPct → null
  suggestedAction → "watch" 默认
  （setup_snapshots 仅覆盖 watchlist；扩到 pool 全集需要单独 sprint）

完成标准：22 条（详见 contract §3）
  含新增 #10b（并发耗时 < 串行 50%）+ #10c（单 ticker FMP 失败不炸全请求）

性能预期（受 token bucket 5 calls/s 稳态约束）：
  trend=50:  ~5–10s
  trend=100: ~15–25s
  trend=200(cap): ~30–40s（vs 串行 80s，约 2× 提升）

F205 sub_sprints 状态：
  F205-a: done
  F205-b: done
  F205-c: contract_agreed ← 当前
  F205-d: planned

[下一个 Session]
  开新 session（建议 Sonnet），粘贴：
  > 继续开发 F205-c，Sprint Contract 已确认。
  > 读取 SESSION-HANDOFF.md + docs/开发/sprint-contracts/F205-c-contract.md，
  > 进入 Generator 模式，从开发步骤 1 开始。

---

[2026-04-27] F205-c PoolService + GET /api/cockpit/pool — DONE

Generator 阶段完成：
- backend/app/schemas/cockpit/pool.py — PoolFunnel / PoolItem / PoolData / PoolResponse
- backend/app/services/cockpit/pool_service.py — PoolService 5 层漏斗（tradable→trend→rs→fundamental→action）
- backend/app/routers/cockpit/pool.py — GET /api/cockpit/pool（参数校验 + PoolService 调用）
- backend/app/routers/cockpit/__init__.py — 注册 pool_router
- backend/tests/test_pool_service.py — 10 单元测试（#8/#9/#10/#10b/#10c/#16/#17/#18）
- backend/tests/test_cockpit_pool_router.py — 16 集成测试（#1-#7/#11-#15/#19-#21）
- docs/系统设计/DECISIONS.md — 追加 D080（ADV 代理/忽略 trendScoreMin/POOL_TREND_CAP/null setup）

全量回归：820 passed（794 → 820，+26），0 失败。
Evaluator 自检清单全部通过（功能/纯净度/命名/决策/代码质量/回归）。
phase: F205-c → needs_review；active_sprint → F205-d（前端 widget，planned）。

[下一个 Sprint]
  F205-d: PoolBuilderWidget 前端，消费 GET /api/cockpit/pool。
  开新 session 继续 F205-d Sprint Contract 阶段。
