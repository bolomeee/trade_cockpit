━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-29] Sprint：F211-c News 页 AI 摘要 bar 前端 — needs_review
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容：
  - newsSummaryUtils.ts 新建（4 pure helpers: stripHtml / sortByPublishedDesc / articlesHash / buildSummarizerArticles）
  - AiNewsSummaryBar.tsx 新建（~330 行，6 态状态机，镜像 AiContradictionsSection）
  - AiNewsSummaryBar.test.tsx 新建（14 tests：C1-C8 component + 6 helper unit tests，14/14 pass）
  - News.tsx 集成（+4 行：import + bar 容器，置于 grid 上方）
  - features.json 更新（sub_sprints F211-c → needs_review，iteration_history 追加）
  - SESSION-HANDOFF.md 更新
  - tsc 0 error；ESLint 0 新增 warning；全量 vitest 273/276（3 个预存 TopNav 失败不变）

📋 预检发现（步骤 1）：
  - --color-surface-muted 不在 tokens.css → chip 背景改用 --color-input-background（存在）
  - queryKey=['news','articles'] ✅ 确认
  - crypto.subtle ✅ Node 24 内置；jsdom DOMParser ✅ vitest environment=jsdom

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-28] Sprint：F211-c News 页 AI 摘要 bar 前端 — Contract 协商完成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容：
  - F211-c Sprint Contract 已确认（用户全采 Q1-Q8 默认方案）
  - features.json：sub_sprints["F211-c"] design_needed → contract_agreed；active_sprint → F211-c
  - iteration_history 追加 F211-c contract_agreed 记录
  - Contract 文件：docs/开发/sprint-contracts/F211-c-contract.md

📦 预计修改文件（4 个，远低于 6 上限）：
  1. frontend/src/components/news/AiNewsSummaryBar.tsx              (新建 ~280 行)
  2. frontend/src/components/news/__tests__/AiNewsSummaryBar.test.tsx (新建 ~11 case)
  3. frontend/src/pages/News.tsx                                    (+6 行：import + bar 容器)
  4. docs/需求/features.json                                          (本次已更新)

🔑 关键约束（开发期不得偏离）：
  - 0 行后端改动（schema/endpoint/guardrail 已就绪，F211-a1 已落地）
  - lazy 触发（默认 collapsed）— 避免每次进 News 页都打 LLM
  - input：articles 来自 useNewsArticles cache，按 publishedAt desc slice 30；windowDays=5（写死）
  - contentText 预处理：DOMParser stripHtml → trim → slice(0, 2000)
  - queryKey: ['ai', 'news_summarizer', deterministicHash]，hash 由 articles {title,publishedAt} 排序后 sha256 前 16 位派生；24h cache 与 trade_plan/contradictions 一致
  - 6 态状态机沿用 AiContradictionsSection（closed/loading/error-502/error-409/success-with/success-with-risks0）
  - success-empty 分支不存在（schema catalystSummary min_length=1）
  - sentiment 颜色 token：positive=--color-success / neutral=--color-text-secondary / negative=--color-error（步骤 1 已预扫，均存在 tokens.css line 28-30）
  - relevantTickers chip 点击调 useAppStore.setSelectedSymbol（与 NewsWidget 一致）
  - 409 guardrail 分支保留（news_summarizer 真有 BANNED_PHRASES，不是模板）
  - articles 为空 → trigger disabled + title="暂无 news"
  - 不持久化 open/closed 状态（每次刷新重置 closed）
  - import 边界：callAiTask 从 @/cockpit/lib/api/aiApi import（pages → cockpit/lib 合法，ESLint 仅约束 cockpit↔workbench 互斥）
  - 不重构 aiApi.ts 上提到 frontend/src/lib/api/（避免触发 4 处 cockpit 内部 import 改动超 6 文件预算）

⚠️ 待 Generator 阶段验证（步骤 1 预检）：
  - jsdom 是否原生支持 DOMParser（throwaway test 验证；不支持需 polyfill 或 mock）
  - crypto.subtle 在 vitest 环境可用性（不可用则 vi.stubGlobal 注入）
  - useNewsArticles queryKey 实际拼写（已预扫 ['news', 'articles']，开发时复核）

📊 功能状态：
  F210（critical AI）：✅ done
  F211（mixed AI）：🟡 in_progress
    F211-a1：✅ done（3 schemas + REGISTRY + guardrail）
    F211-a2：✅ done（per-task model override 基建 + D075）
    F211-b：✅ done（DecisionPanel Contradictions 区前端，验收通过）
    F211-c：🟡 contract_agreed（News 页 AI 摘要 bar ← 本次）
    F211-d：⬜ design_needed（平仓 hook + ai_review 迁移 + 月度 cron）
  F206-c2：✅ done

[下一个 Session]
  建议用 Sonnet 开新 session，粘贴：

  > 继续开发 F211-c，Sprint Contract 已确认。
  > 读取 SESSION-HANDOFF.md + docs/开发/sprint-contracts/F211-c-contract.md，
  > 进入 Generator 模式，从开发步骤 1（预检：grep tokens + 复核 useNewsArticles queryKey + 验证 jsdom DOMParser/crypto.subtle）开始。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-28] Sprint：F206-c2 PendingOrdersWidget — Contract 协商完成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容：
  - F206-c2 Sprint Contract 已确认（用户全意 §7 6 项）
  - 修正 features.json drift：F206 之前被错标 status:done/completed_at:2026-04-26，实际 c2 未做即跳到 F207
  - 重置 F206 status: in_progress / phase: contract_agreed / active_sub_sprint: F206-c2
  - 顶层 _pipeline_status: active_sprint=F206-c2, phase=contract_agreed
  - 合约 frontmatter: drafted → confirmed
  - SESSION-HANDOFF.md 已生成

📋 已确认决策（待 Generator 阶段落地到 DECISIONS.md）：
  - D060-a：v1.9 PendingOrder [Triggered] 后不自动创建 Position，仅切 status + toast 提示用户去 Positions widget 手工录入
  - distance 颜色按绝对值阈值上色：|x|>5% 灰 / 1≤|x|≤5 默认 / |x|<1 加粗
  - Edit dialog 不暴露 status；status 转换只走行按钮 [Triggered]/[Cancel]
  - [Cancel] 直接 PATCH 无确认；[Triggered] 弹确认 dialog + toast
  - defaultLayout (x:6, y:8, w:6, h:8) 与 c1 PositionList 同行右侧
  - category 复用 'position'，不新增 'order'

📦 预计修改文件（6 生产 + 3 测试，正好压在 6 文件上限）：
  生产：
    1. frontend/src/cockpit/lib/api/cockpitPendingOrdersApi.ts (新建)
    2. frontend/src/cockpit/widgets/PendingOrdersWidget.tsx (新建)
    3. frontend/src/cockpit/widgets/_pendingOrderRow.tsx (新建)
    4. frontend/src/cockpit/dialogs/PendingOrderFormDialog.tsx (新建)
    5. frontend/src/cockpit/dialogs/_pendingOrderFormSchemas.ts (新建)
    6. frontend/src/cockpit/CockpitRegistry.ts (修改)
  测试：
    T1. frontend/src/cockpit/lib/api/__tests__/cockpitPendingOrdersApi.test.ts
    T2. frontend/src/cockpit/widgets/__tests__/PendingOrdersWidget.test.tsx
    T3. frontend/src/cockpit/dialogs/__tests__/PendingOrderFormDialog.test.tsx

⚠️ 待你决策：
  - 无（合约已全意确认）

🔧 系统变更：
  - features.json：F206 status drift 修正 + F206-c2 contract_agreed 落地
  - F206-c2-contract.md frontmatter：drafted → confirmed

📊 功能状态：
  F205：✅ done（a/b/c/d/e）
  F206：🟡 in_progress（a/b1/b2/c1 done，c2 contract_agreed）
  F207：✅ done（a/b）— 注：在 F206-c2 之前完成（drift 期间）

[下一个 Session]
  建议用 Sonnet 开新 session，粘贴：

  > 继续开发 F206-c2，Sprint Contract 已确认。
  > 读取 SESSION-HANDOFF.md + docs/开发/sprint-contracts/F206-c2-contract.md，
  > 进入 Generator 模式，从开发步骤 1 开始。


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-28] Sprint：F211-a1 — Generator 完成（Sonnet 4.6）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容：
  - 新建 backend/app/ai/schemas/contradiction_detector.py（+87 行）
  - 新建 backend/app/ai/schemas/news_summarizer.py（+74 行）
  - 新建 backend/app/ai/schemas/journal_assistant.py（+188 行，含 model_validator 双模式）
  - 修改 backend/app/ai/schemas/__init__.py：REGISTRY 补 3 entry + guardrail.register 3 行（替换占位注释）
  - 新建 backend/tests/test_ai_schemas_f211a1.py：45 单元测试（CI/CO/CG/NI/NO/NG/JI/JO/JG/R1-R7）全绿
  - 追加 backend/tests/test_ai_gateway_e2e_f208c.py：C13a+C13b 两条集成测试全绿
  - 全量回归：877 passed，0 failed
  - mypy backend/app/ai/schemas/：no issues found in 8 source files
  - smoke：3 F211 task_types 在 REGISTRY，启动无 import 错误
  - features.json：F211-a1 升 done，iteration_history 补条目
  - 3 wip commit（5de6459 / b642bf8 / 6e83744）

📊 功能状态：
  F211-a1：✅ done
  F211-a2：⬜ design_needed（per-task model override 基建）
  F211-b/c/d：⬜ design_needed（前端 + 月度 cron）
  F206-c2：🟡 contract_agreed（PendingOrdersWidget，独立 sprint，无冲突）

🔧 wip commits：
  5de6459  wip(F211-a1): 3 schema files + 45 unit tests
  b642bf8  wip(F211-a1): registry wiring
  6e83744  wip(F211-a1): e2e C13 gateway integration

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-28 Sprint Contract] Sprint：F211-a1 AI Contradiction Detector + News Summarizer + Journal Assistant 后端 schemas
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容（本 session）：
  - 读完前置文档（CLAUDE.md / API-CONTRACT §AI / DATA-MODEL §AiMemo + JournalEntry / design-spec §6.d + §9 / data-mapping §6.d）+ 先例 F210-a/b/c contract
  - 扫码盘点 F211 整体范围（15+ 文件，必须拆分）
  - 与用户协商拆分方案：5 段（a1 后端 schemas / a2 per-task model override 基建 / b DecisionPanel Contradictions / c News 页 AI 摘要 bar / d 平仓 hook + ai_review 迁移 + 月度 cron）
  - 用户确认 5 段拆分 + Q1-Q4 默认方案（含 news 摘要范围 / journal hook 语义 / 月度产出形态）
  - 用户加码：F211-a2 必须支持 per-task 自定义模型 + base_url + token 单价；摘要 UI 必须显示 tokens + cost
  - 解释 D070 line 1527：AI 模型走 .env 不进 cockpit_params.py，明确 a2 落 config.py 而非 cockpit_params.py
  - 更新 features.json：F211 sub_sprints 新增 5 entry（a1/a2/b/c/d）
  - 调用 consistency-check skill (mode=interactive)：C5/C6/C8 全清，无阻塞
  - 起草 F211-a1 Sprint Contract（docs/开发/sprint-contracts/F211-a1-contract.md）
    范围：3 schema 文件 + REGISTRY 修改 + 测试 1 文件 = 5 文件；~38 单元测试 + 1 集成
    关键设计：journal_assistant 用 mode 字段 + model_validator 实现双模式（trade/monthly），单一 task_type 单一 REGISTRY entry
    BANNED_PHRASES guardrail 沿用 F209-a 6 个标准 phrase；3 task 均无 deterministic 锚点
    earningsRisk 接受 None（参 F210-c 教训）
  - 用户确认 Contract，6 个开放问题（Q1-Q6）全采默认方案
  - 更新 features.json：F211-a1 升 contract_agreed + iteration_history 补一条
  - phase → contract_agreed

⚠️ 待你决策：
  - 无（Contract 全意确认；Q1-Q6 默认方案）

🔧 系统变更：
  - features.json：F211 新增 sub_sprints + sub_sprint_notes + iteration_history（F211-a1 entry）
  - 新增 docs/开发/sprint-contracts/F211-a1-contract.md

📊 功能状态：
  F210：✅ done（a/b/c）
  F211：🟡 contract_agreed（a1 contract_agreed；a2/b/c/d design_needed）
    F211-a1：🟡 contract_agreed（待 Generator）
    F211-a2：⬜ design_needed（per-task model override 基建，依赖 a1 后可起草）
    F211-b：⬜ design_needed（DecisionPanel Contradictions 区前端）
    F211-c：⬜ design_needed（News 页 AI 摘要 bar 前端，含 tokens/cost 展示）
    F211-d：⬜ design_needed（平仓 hook + ai_review 迁移 + 月度 cron）

[下一个 Session]
  建议用 Sonnet 开新 session，粘贴：

  > 继续开发 F211-a1，Sprint Contract 已确认。
  > 读取 SESSION-HANDOFF.md + docs/开发/sprint-contracts/F211-a1-contract.md，
  > 进入 Generator 模式，从开发步骤 7a 开始（contradiction_detector schema + tests）。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-28] Sprint：F211-a2 — Per-task Model Override 基建（Architect 模式 / Contract 协商）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容：
  - Architect 模式起草 F211-a2 Sprint Contract（docs/开发/sprint-contracts/F211-a2-contract.md）
    范围：D064 三 tier 兜底之上叠加可选 per-task override，env-driven 单 JSON 字段
    5 文件：config.py（+1 字段）/ routing.py（新 ResolvedRoute + 升 resolve()）/ gateway.py（透传 api_base + lazy register_model）/ test_ai_routing_overrides.py（新建 13 用例）/ DECISIONS.md（追加 D075）
    测试扩展（不计入 5 文件）：test_ai_core_modules_f208b 1 处适配；test_ai_gateway_e2e_f208c 追加 C13c override e2e
    关键设计：register_model 在 gateway lazy hook（不放 main.py lifespan）；api_key fallback 到 settings.openai_api_key；cost 单位 per-1M（register 前 / 1_000_000）；fail-soft（JSON 坏 / register 失败 → log warning + fallback，不阻塞 startup）
    向后兼容：resolve_tier / resolve_model 旧 API 保留；未配 override 时 877 测试零回归
  - 用户确认 Contract，7 个开放问题（Q1-Q7）全采默认方案
    Q1/Q2 强 context7 查询点（Generator 步骤 1）：litellm.completion 形参 api_base vs base_url；register_model 后 completion_cost 是否用注入价
  - 更新 features.json：F211-a2 升 contract_agreed + iteration_history 追加一条
  - phase → contract_agreed

⚠️ 待你决策：
  - 无（Contract 全意确认；Q1-Q7 默认方案）

🔧 系统变更：
  - 新增 docs/开发/sprint-contracts/F211-a2-contract.md
  - features.json#F211：sub_sprints["F211-a2"] design_needed → contract_agreed

📊 功能状态：
  F210：✅ done（a/b/c）
  F211：🟡 in_progress
    F211-a1：✅ done
    F211-a2：🟡 contract_agreed（待 Generator）
    F211-b：⬜ design_needed（DecisionPanel Contradictions 区前端）
    F211-c：⬜ design_needed（News 页 AI 摘要 bar 前端，含 tokens/cost 展示）
    F211-d：⬜ design_needed（平仓 hook + ai_review 迁移 + 月度 cron）
  F206-c2：🟡 contract_agreed（PendingOrdersWidget 前端，独立可推进）

[下一个 Session]
  建议用 Sonnet 开新 session，粘贴：

  > 继续开发 F211-a2，Sprint Contract 已确认。
  > 读取 SESSION-HANDOFF.md + docs/开发/sprint-contracts/F211-a2-contract.md，
  > 进入 Generator 模式，从开发步骤 1（context7 查 LiteLLM 形参与 register_model 行为）开始。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-28] Sprint：F211-b DecisionPanel Contradictions 区前端 — Contract 协商完成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容：
  - F211-b Sprint Contract 已确认（用户全意 Q1-Q10 默认方案）
  - features.json：sub_sprints["F211-b"] design_needed → contract_agreed
  - iteration_history 追加 F211-b contract_agreed 记录
  - Contract 文件：docs/开发/sprint-contracts/F211-b-contract.md

📦 预计修改文件（5 个，≤6 上限）：
  1. frontend/src/cockpit/components/AiContradictionsSection.tsx     (新建 ~250 行)
  2. frontend/src/cockpit/widgets/DecisionPanelWidget.tsx            (修改 ~10 行 diff)
  3. frontend/src/cockpit/widgets/__tests__/DecisionPanelWidget.test.tsx  (+7 case)
  4. frontend/src/cockpit/lib/utils/dates.ts                         (新建，calcDaysUntil 抽取)
  5. docs/需求/features.json                                          (本次已更新)

🔑 关键约束（开发期不得偏离）：
  - 0 行后端改动（schema/endpoint/guardrail 已就绪）
  - lazy 触发（默认 collapsed）— 避免每次 selectedTicker 切换都打 LLM
  - input 13 字段：8 个来自 CockpitDecisionData / 5 个（trendScore / rsPercentile / readySignal / regime / regimeScore）从 ['setup-monitor'] + ['cockpit-regime'] react-query cache 复用
  - setupMonitor 缺当前 ticker → trigger disabled + tooltip
  - severity tag 颜色 token：HIGH=--color-error / MEDIUM=--color-signal-warning / LOW=--color-text-muted（Generator 步骤 1 须 grep tokens.css 实测）
  - queryKey: ['ai', 'contradiction_detector', ticker, deterministicHash]，24h cache 与 trade_plan 一致
  - 6 态状态机沿用 AiTradePlanSection（closed / loading / success-with / success-empty / error 502 / error 409）
  - 即使 contradiction_detector 后端无 hash guardrail，前端保留 409 分支模板

⚠️ 待 Generator 阶段验证（步骤 1 预检）：
  - tokens.css 是否实存 --color-signal-warning（不存在则切换到 --color-warning / --color-amber）
  - SetupMonitorWidget 当前 queryKey 精确写法（['setup-monitor'] vs ['setup-monitor', undefined]）

📊 功能状态：
  F210（critical AI）：✅ done
  F211（mixed AI）：🟡 in_progress
    F211-a1：✅ done（3 schemas + REGISTRY + guardrail）
    F211-a2：✅ done（per-task model override 基建 + D075）
    F211-b：🟡 contract_agreed（DecisionPanel Contradictions 区前端 ← 本次）
    F211-c：⬜ design_needed（News 页 AI 摘要 bar 前端）
    F211-d：⬜ design_needed（平仓 hook + ai_review 迁移 + 月度 cron）
  F206-c2：🟡 contract_agreed（PendingOrdersWidget 前端，独立可推进）

[下一个 Session]
  建议用 Sonnet 开新 session，粘贴：

  > 继续开发 F211-b，Sprint Contract 已确认。
  > 读取 SESSION-HANDOFF.md + docs/开发/sprint-contracts/F211-b-contract.md，
  > 进入 Generator 模式，从开发步骤 1（预检：grep tokens + 确认 setup-monitor / cockpit-regime queryKey + 读 backend contradiction_detector schema）开始。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-28 16:15] Sprint：F211-b DecisionPanel Contradictions 区前端
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容：
  - AiContradictionsSection.tsx 新建（~285 行，6 态状态机镜像 AiTradePlanSection）
  - cockpit/lib/utils/dates.ts 新建（calcDaysUntil 从 DecisionPanelWidget 抽取，DRY）
  - DecisionPanelWidget.tsx 修改（import 切换 + 追加 contradictions divider/section，~15 行 diff）
  - DecisionPanelWidget.test.tsx 新增 8 case（C1-C8），29/29 通过
  - 全量回归：259/262（3 预先存在失败 TopNav，非本 sprint 引入）
  - tsc --noEmit 0 error；ESLint 我变更文件 0 新增 warning
  - phase → needs_review

⚠️ 预检调整（合约内置 fallback，Q4/Q5）：
  - MEDIUM severity token：--color-signal-warning 不存在 → 改用 --color-log-warn (#f59e0b)
  - setup-monitor queryKey：['setup-monitor', undefined] → ['cockpit-setup-monitor', 'all']

🔧 系统变更：
  - 新建 frontend/src/cockpit/components/AiContradictionsSection.tsx
  - 新建 frontend/src/cockpit/lib/utils/dates.ts
  - 修改 frontend/src/cockpit/widgets/DecisionPanelWidget.tsx
  - 修改 frontend/src/cockpit/widgets/__tests__/DecisionPanelWidget.test.tsx
  - 修改 docs/需求/features.json（F211-b → needs_review）

📊 F211 进度：
  F211-a1：✅ done
  F211-a2：✅ done
  F211-b：🔍 needs_review
  F211-c：⬜ design_needed
  F211-d：⬜ design_needed

[下一步]
  运行 acceptance skill 验收 F211-b；或继续 F211-c（News 页 AI 摘要 bar）。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-04-28] F206-c2 状态漂移修正
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 发现：F206-c2 已在之前 session 完整实现并提交（commits 4082834→6958fde），
但 a8d3445 误将 features.json 重置为 contract_agreed。本次 Evaluator 自检确认：

✅ 文件：9 文件全部存在（6 生产 + 3 测试），行数全部 ≤ 350
✅ 测试：S1–S20 全过，F206-c2 专项 41/41
✅ 全量回归：259/262（3 预先存在 TopNav 失败）
✅ 无硬编码 hex（S19 静态检查）
✅ CockpitRegistry 已注册 cockpit.pending-orders
✅ DECISIONS.md D060-a 已存在
✅ design-spec §Widget 8 待决策项已标注 D060-a

🔧 本次变更：
  - 修正 docs/需求/features.json（F206-c2 → done，F206 status → done）

📊 F206 状态：
  F206-a：✅ done
  F206-b1：✅ done
  F206-b2：✅ done
  F206-c1：✅ done
  F206-c2：✅ done（F206 收尾完成）

---
2026-04-28 | F211-b 验收通过

AiContradictionsSection（DecisionPanel Contradictions 区）验收通过。
- 6 态状态机：closed / loading / error-502 / error-409 / success-with / success-empty
- 29/29 vitest，tsc 0 error，0 新增 lint error
- calcDaysUntil 已提取到 cockpit/lib/utils/dates.ts（F211-c/d 可复用）
- MEDIUM severity 使用 --color-log-warn（--color-signal-warning 缺失）
- features.json：F211-b → done；F207.status → done（C1 顺带修复）
- 验收记录：docs/验收/v1.9-F211-b-acceptance.md

F211 进度：a1 ✅ a2 ✅ b ✅ c ⬜ d ⬜
