=== PROJECT PROGRESS LOG ===
项目：MA150 Tracker → Workbench

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-07-02] Pipeline 阶段 0：迭代需求登记（v2.8）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 本次完成：
  - project-init 迭代模式：新增 F222（Watchlist 颜色标记）
  - 更新 docs/需求/PRD.md「版本迭代记录」章节（v2.8）
  - 更新 docs/需求/features.json：追加 F222（phase=design_needed）、_iteration_log、_pipeline_status.current_iteration=v2.8

📊 F222 摘要：
  Watchlist ticker 左侧圆形色块按钮 + Popover 四色选择（红/黄/蓝/无色），
  后端持久化（Stock.label_color），CSV 导出携带颜色列，导入/新增默认无色。
  跳过完整 design-bridge（无新 Figma 稿），system-design 阶段直接补
  design-spec.md 颜色 token。

[下一步]
  触发 system-design：补 DATA-MODEL.md（Stock.label_color）+
  API-CONTRACT.md（PUT /api/watchlist/{ticker}/color，GET /api/signals
  新增 labelColor）+ component-plan.md + design-spec.md 颜色 token 章节

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-07-02] Pipeline 阶段 1：system-design 补文档（F222）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 本次完成（跳过 design-bridge，system-design 直接登记）：
  - DATA-MODEL.md：Stock 新增 label_color 字段（String(10) nullable，
    red/yellow/blue/null，默认 null）+ 枚举值表
  - API-CONTRACT.md：新增 PUT /api/watchlist/{ticker}/color；
    GET /api/signals 响应新增 labelColor 字段
  - docs/设计/component-plan.md：追加 §F222 ColorTagButton / ColorTagPopover
    组件边界
  - docs/设计/design-spec.md：追加 §F222 颜色 token（--color-label-red/
    yellow/blue，全部别名到既有 token）+ 视觉规格
  - DECISIONS.md：新增 D110（字段/挂载点/token 设计 + 不设 dark 覆盖，
    理由引用 D109 既有惯例）
  - features.json：F222 phase design_needed → ready_to_dev；
    _pipeline_status.system_design / ui_design → done

⚠️ 与 SESSION-HANDOFF.md 原指令的一处偏离（已在 DECISIONS.md D110 说明）：
  未给 label 颜色 token 设置 dark mode 覆盖。原因：D109 已确立"饱和语义色
  （signal/action/log/regime/setup）在 tokens-dark.css 中有意不覆盖"的
  惯例（暗底本身可读 + 绑定 DATA-MODEL 枚举），label-* 属同类色，沿用
  该惯例而非另起例外。如 feature-dev 阶段发现暗色主题下可读性问题，
  再补 tokens-dark.css 覆盖。

[下一步]
  进入 feature-dev：协商 F222 Sprint Contract（后端字段+迁移+端点，
  前端 ColorTagButton/Popover + WatchlistWidget 接入 + CSV 导出列）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-07-02] Pipeline 阶段 2：F222 Sprint Contract 协商（F222-a）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ 前序门禁扫描发现 F222 预计修改 14 个文件（backend 9 + frontend 5），
  超出 feature-dev 6-file 上限。用户确认拆分为 3 个 sub-sprint：
    F222-a：backend 读路径（4→5 文件，含新测试文件）
    F222-b：backend 写路径（5 文件）
    F222-c：frontend（5 文件）
  依赖顺序 a → b → c（b 需要 a 的列存在；c 需要 a+b 的 API 就绪）。

✅ 本次完成：
  - features.json：F222 新增 sub_sprints 字段（a/b/c），consistency-check
    C5 验证通过（sub_sprints ↔ 合约目录双向一致，0 违例）
  - F222-a Sprint Contract 已协商 + 用户 ack + 确认：
    docs/开发/sprint-contracts/F222-a-contract.md（status: confirmed）
  - F222-a 范围：Stock.label_color 列 + alembic migration 026 +
    SignalBoardItem.labelColor 字段 + signal_service 传值；
    5 个文件，全部 backend，测试文件
    test_watchlist_label_color_f222a.py（migration 升降级 cycle +
    GET /api/signals labelColor 字段测试）
  - features.json：F222.status → in_progress，
    sub_sprints['F222-a'] → contract_agreed，
    _pipeline_status.active_sprint → "F222-a"

[下一步]
  开新 session，进入 Generator 模式开发 F222-a（开发步骤 1：
  Stock 模型加 label_color 列 → migration → schema → service → 测试）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-07-02] Pipeline 阶段 3：F222-a Generator + Evaluator 完成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 本次完成（Contract §2 五文件全部落地，逐步 wip commit）：
  - backend/app/models/stock.py：Stock 新增 label_color = Column(String(10), nullable=True)
  - backend/alembic/versions/026_f222_stock_label_color.py：新迁移，
    down_revision=025_f219a_setup_macd_divergence，upgrade 加列/downgrade 删列，不回填
  - backend/app/schemas/signal.py：SignalBoardItem 新增
    label_color: Literal["red","yellow","blue"] | None = None（序列化 labelColor）
  - backend/app/services/signal_service.py：_build_board_item() 新增 "labelColor": stock.label_color
  - backend/tests/test_watchlist_label_color_f222a.py：新增 5 用例（alembic 升降级 3 段
    cycle + GET /api/signals labelColor 默认 null / 显式值透传），全过

✅ Evaluator 自检（Contract §6 逐条）：
  - pytest 新测试文件：5/5 通过
  - alembic upgrade head → downgrade -1 → upgrade head：在真实 dev.db 上跑通，三段干净
  - 全量回归：1302 passed，0 新增失败
  - ruff：本 sprint 5 文件全过
  - API-CONTRACT.md / DATA-MODEL.md 一致性：已核对（GET /api/signals.labelColor 字段名/类型/位置
    与文档一致；GET /api/watchlist、GET /api/signals/:ticker 均未误动，符合 Contract §1.5 排除范围）
  - 无 print/调试残留；ruff 报告的 2 处死代码（sqlalchemy.text / SetupSnapshotRepository 未用 import）
    经核实为 F219-a 遗留、与本次改动无关，按规则 3 不清理
  - 修改文件严格等于 Contract §2 清单（5 个，无新增无遗漏）

⚠️ 连带修复（不在 Contract §2 内，本次 migration 新增导致的既有回归测试失败，
  已征得用户同意，各自独立 fix commit，不计入 F222-a feat commit）：
  - backend/tests/test_schema.py：EXPECTED_COLUMNS["stocks"] 补 label_color
    （该测试职责是追踪 schema drift，DATA-MODEL.md 已定义此字段但常量未同步）
  - backend/tests/test_setup_macd_f219a.py：两处 downgrade(cfg, "-1") 改为显式
    revision id "f218_d6a_fundamentals_quarterly"（相对 -1 假设 025 是 head，
    026 追加后该假设失效）。技术决策见 DECISIONS.md D111。

  同时补提交一处上个 session 遗留的 SESSION-HANDOFF.md 收尾编辑
  （补全 checksum 表 sha，docs 类型 commit）。

✅ features.json：F222.sub_sprints['F222-a'] contract_agreed → done；
  _pipeline_status.active_sprint_phase → "done"；iteration_history 追加 done 记录

[下一步]
  F222-b（backend 写路径：PUT /api/watchlist/{ticker}/color +
  schemas/watchlist.py UpdateColorRequest + stock_repository.set_label_color +
  watchlist_service.update_color + routers/watchlist.py 路由 + 测试，预计 5 文件）
  进入 Sprint Contract 协商

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-07-02] Pipeline 阶段 4：F222-b Sprint Contract 协商
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ 前序门禁检查：F001 done ✓、F222-a done ✓，均满足门禁，直接进入协商。

✅ 本次完成：
  - F222-b Sprint Contract 已协商 + 用户 ack + 确认：
    docs/开发/sprint-contracts/F222-b-contract.md（status: confirmed）
  - F222-b 范围：新增 PUT /api/watchlist/{ticker}/color 写入口——
    schemas/watchlist.py 新增 UpdateColorRequest（color: Literal["red","yellow","blue"]|None，
    必填无 default）+ UpdateColorResponse；stock_repository.py 新增
    set_label_color(stock, color)（mutate+commit+refresh，同 soft_delete 模式）；
    watchlist_service.py 新增 update_color(ticker, color)（NOT_FOUND 判定沿用
    remove_stock 同款逻辑：行不存在或 is_active=false）；routers/watchlist.py
    新增路由；test_watchlist_api.py 追加 TC1–TC7 共 7 用例。5 个文件，全部
    backend，无新增文档改动（system-design 阶段已完成读写两路径定义）。
    无技术决策分叉，全部沿用 F222-a 已确立的代码惯例。
  - features.json：F222.sub_sprints['F222-b'] ready_to_dev → contract_agreed，
    _pipeline_status.active_sprint → "F222-b"，active_sprint_phase → "contract_agreed"，
    iteration_history 追加 contract_agreed 记录

[下一步]
  开新 session，进入 Generator 模式开发 F222-b（开发步骤 1：
  schemas/watchlist.py 加 UpdateColorRequest/UpdateColorResponse →
  repository → service → router → 测试）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-07-02] Pipeline 阶段 5：F222-b Generator + Evaluator
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ Generator 入口 pre-flight：核对 SESSION-HANDOFF.md checksum 表（contract /
  features.json 均落在 commit 0fbd917，HEAD 已推进到 a121be6，符合"可推进不可早于"
  约束）；consistency-check（interactive，scope=F222-b，C5/C10/C11）：C5 双向一致 ✓、
  C10 phase 转移+dependency gating ✓（F001 done、F222-a done）、C11 因项目无
  tools/sdlc-doctor.py 优雅跳过。全清后进入 Generator。

✅ 开发步骤（按 Contract §1.1→§1.5 顺序，每步 wip commit）：
  - backend/app/schemas/watchlist.py：新增 UpdateColorRequest(color: Literal
    ["red","yellow","blue"]|None，无 default) + UpdateColorResponse
  - backend/app/repositories/stock_repository.py：新增 set_label_color(stock, color)
    （mutate+commit+refresh，同 soft_delete/reactivate 模式）
  - backend/app/services/watchlist_service.py：新增 update_color(raw_ticker, color)
    （ticker 归一化 + NOT_FOUND 判定沿用 remove_stock 同款逻辑）
  - backend/app/routers/watchlist.py：新增 PUT /{ticker}/color 路由
  - backend/tests/test_watchlist_api.py：追加 TC1–TC7（7 用例：设红/清空/大小写
    不敏感/非法枚举/未加入/已软删/缺字段），全过

✅ Evaluator 自检（Contract §6 逐条）：
  - pytest tests/test_watchlist_api.py：35/35 通过（含 TC1–TC7）
  - 全量回归：1309 passed, 11 deselected，0 新增失败（含 F222-a 5 例）
  - API-CONTRACT.md §Watchlist PUT .../color：字段名/类型/错误码逐条核对一致
  - DATA-MODEL.md：label_color 字段命名一致（复用 F222-a 已定义列，未改 schema）
  - 无 print/调试残留；无死代码；ruff 对本 sprint 5 文件全过；mypy 项目未配置，跳过
  - 实现范围严格等于 Contract §1（未碰 GET 读路径、未碰任何前端文件）
  - 修改文件严格等于 Contract §2 清单（5 个，git diff 核对一致）

✅ features.json：F222.sub_sprints['F222-b'] in_progress → done；
  _pipeline_status.active_sprint_phase → "done"；iteration_history 追加 done 记录。
  父 feature F222 status 保持 in_progress（F222-c 仍 ready_to_dev，未触发 C1 父完成
  invariant）。

[下一步]
  F222-c（frontend：ColorTagButton + Popover 交互 + WatchlistWidget 接入 +
  API client + CSV 导出携带颜色列）进入 Sprint Contract 协商，
  a + b 都 done 后方可真正联调。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-07-02 18:26] Sprint:F222-c Watchlist 颜色标记（frontend）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 本次完成：
  - F222-c Sprint Contract 已协商 + 用户 ack + 确认：
    docs/开发/sprint-contracts/F222-c-contract.md（status: confirmed）
  - F222-c 范围：ColorTagButton + Popover 前端接入——
    tokens.css 新增 --color-label-red/yellow/blue 3 个别名 token（发现
    design-spec.md 已登记但实现层遗漏，本次补齐，同步更新 features.json 的
    estimated_files_changed）；types/signal.ts 新增 LabelColor 类型 +
    SignalBoardItem.labelColor 字段；lib/api/watchlist.ts 新增 updateColor；
    components/features/dashboard/ColorTagButton.tsx 新建（含内部
    ColorTagPopover 子组件，不单开文件，事件冒泡隔离——阻止色块点击穿透到行的
    onSelect）；WatchlistWidget.tsx 接入色块按钮 + mutation + CSV 导出加颜色列；
    WatchlistWidget.test.tsx 新建（TC1–TC9，仅覆盖颜色标记相关行为，不补齐
    delete/import/add 等既有功能回归测试）。6 个文件，全部 frontend。
  - 协商中与用户确认两点：① 颜色更新失败（非 NOT_FOUND）用 sonner toast 提示，
    NOT_FOUND 静默刷新；② CSV 未标记行写字面值 none，颜色列追加在 name 之后。
    另有 2 处纯实现细节技术决策（Popover 关闭机制用 radix-ui 原语、事件冒泡
    隔离范围）未单独征询用户，Evaluator 通过后一并追加 DECISIONS.md。
  - features.json：F222.sub_sprints['F222-c'] ready_to_dev → contract_agreed，
    _pipeline_status.active_sprint → "F222-c"，active_sprint_phase →
    "contract_agreed"，iteration_history 追加 contract_agreed 记录

📊 功能状态：
  F222 Watchlist 颜色标记：🔧 in_progress（F222-a done / F222-b done / F222-c contract_agreed）

[下一步]
  开新 session，进入 Generator 模式开发 F222-c（开发步骤 1：
  tokens.css 加 3 个 --color-label-* token → types/signal.ts →
  lib/api/watchlist.ts → ColorTagButton.tsx → WatchlistWidget.tsx →
  WatchlistWidget.test.tsx）。全部完成后 F222 三个 sub-sprint 齐活，
  触发 acceptance。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2026-07-03 10:29] Sprint:F222-c Watchlist 颜色标记（frontend）— Generator + Evaluator 完成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完成内容：
  - tokens.css 新增 --color-label-red/yellow/blue 3 个别名 token
  - types/signal.ts 新增 LabelColor 类型 + SignalBoardItem.labelColor 字段
  - lib/api/watchlist.ts 新增 updateColor(ticker, color)
  - ColorTagButton.tsx 新建（含内部 ColorTagPopover：radix-ui Popover.Close
    原语关闭机制 + 触发按钮容器/PopoverContent 双重 stopPropagation 事件隔离）
  - WatchlistWidget.tsx 接入色块按钮 + colorMutation（NOT_FOUND 静默刷新，
    其余错误 sonner toast）+ CSV 导出加颜色列（name 之后，null→"none"）
  - WatchlistWidget.test.tsx 新建，TC1–TC9 全部覆盖颜色标记相关行为
  - 单元测试：9/9 通过（WatchlistWidget 专项）
  - 全量回归：374 passed，无新增失败
  - npm run build：通过，无 TS 类型错误
  - 真实前后端联调（源码临时 uvicorn + 真实 dev.db）：色块渲染/Popover 4 色块/
    选色写入 PUT /api/watchlist/{ticker}/color/事件隔离（点击色块不触发行选中，
    Price Chart 面板未跳转）/CSV 导出内容抓取校验/清除标记 全部浏览器实测通过，
    测试数据已清理归零（AVGO/CIM/F 恢复 null）
  - Evaluator 自检清单：全部通过（含"实现范围严格等于 Contract §1"、
    "修改文件严格等于 Contract §2 清单 6 个"两项范围检查）
  - DECISIONS.md 追加 D112（Popover 关闭机制 / 错误提示 / CSV 空值 / 事件冒泡隔离）
  - consistency-check（C1+C4+C5，scope=F222）：0 违例
  - phase → F222.sub_sprints['F222-c']: in_progress → done；
    F222.phase: ready_to_dev(stale) → needs_review；F222.status 保持
    in_progress（C1 invariant：acceptance 未跑，不自动升 done）

📊 功能状态：
  F222 Watchlist 颜色标记：🔍 needs_review（F222-a done / F222-b done / F222-c done，等待 acceptance）

[下一步]
  触发 acceptance skill，对 F222 五条 acceptance_criteria 做最终验收
  （含真实前后端联调，非 mock——F222-c 开发期间已完成过一轮真实联调验证，
  acceptance 阶段可直接复用该验证结论或按需重跑）。acceptance 通过后
  F222.status 才可标记 done。
